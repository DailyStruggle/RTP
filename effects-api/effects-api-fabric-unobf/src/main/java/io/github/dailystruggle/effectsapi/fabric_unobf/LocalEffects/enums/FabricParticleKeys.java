package io.github.dailystruggle.effectsapi.fabric_unobf.LocalEffects.enums;

/** Per-effect data keys for {@code FabricParticleEffect}. Order = ADR-002 reading order. */
public enum FabricParticleKeys { TYPE, COUNT, DX, DY, DZ, SPEED }
