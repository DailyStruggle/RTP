# Changelog

All notable changes to RTP are documented here.

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.0.0/); versioning follows [SemVer](https://semver.org/spec/v2.0.0.html).

---

## [3.0.0-beta.4] - Unreleased

### Added

- **Admin-panel curated prefabs subsystem.** New `/rtp admin` shim opens the curated admin panel (`OpenAdminPanel`, gated on `rtp.menu.admin`) with a Setup section listing five bundled prefabs (`survival-default`, `low-performance`, `high-performance`, `folia-tuned`, `multi-world`) under `rtp.admin.prefab`. Each row opens a confirmation page (`PrefabConfirmationMenuBuilder`) backed by a ~60s caller-bound single-use nonce; `Confirm` writes via `PrefabDiskIO.writeWithBackup` with `.bak.<epochMillis>` retention (`prefab.bakRetention` knob, default 3) plus best-effort reload. Matching chat surface `/rtp admin prefab {list,apply,confirm,rollback}`; pure-function core under `rtp-core/.../commands/prefab/`. 15 `MessagesKeys` constants for the Setup section propagated through the locale TSV pipeline. Covered by 7 test classes; new admin-facing `docs/admin/PREFABS.md`.
- **Network mode shared cross-server waitlist** (rtp-proxy-ADR-015, REQ-RTP-NET-015). Mirrors the per-backend `playerQueue` UX across proxies in five slices: (1) `NetworkWaitlist` SPI + `InMemoryNetworkWaitlist` reference impl (LinkedHashMap-backed, `maxSize` default 1024); (2) `NetworkWaitlistDrainer` + `DefaultRtpDispatcher` rewiring + new `WAITLISTED` `QueueState` + `WaitlistLeaderLease` SPI; (3a) Redis-backed `RedisLeaderLease` (NX/XX PX, compare-and-DEL) for cross-proxy single-leader election; (3b) `RedisNetworkWaitlist` with 7 atomic Lua scripts (enrol, drain-batch, position, remove, reap, refresh-ttl, etc.); (4) lobby-side UX wiring (notify, command-lock, quit-removal); (5) `MessagesKeys` parity: new `alreadyQueued` key + `[position]` placeholder propagated through every shipped locale. Covered by `InMemoryNetworkWaitlistTest` (15), `DefaultRtpDispatcherWaitlistTest` (4), opt-in `RedisLeaderLeaseIT` (8), `RedisNetworkWaitlistIT`, and `LocaleParityTest`.
- **Lobby load balancing v1 (most-kept) documented.** A backend booted with `network.yml::routing.lobbyMode: true` advertises `acceptingRequests=false` and `regionsAvailable=Set.of()`; bare `/rtp` on it is intercepted by `BukkitNetworkCommand` and routed to the peer with the most kept locations. Code shipped earlier as Slices I+J during the cross-server `/rtp` stabilisation; this entry documents the design, sequencing, and coverage (`LobbyModeTest` 12, `LobbyModeEarlyReadTest` 3).
- **Declarative chart composition bridge (ADR-047, REQ-RTP-MAP-006).** Renderer-neutral chart-spec + resolver SPI plumbed end-to-end across multiple stages: governance row in `TRACEABILITY.md`/REQUIREMENTS.md §1.7; `BukkitMapBinding` first concrete `MapBinding` under `maps-api/.../mapsapi/bukkit/` (allocate/renderEphemeral); `MapBindingLifecycle` SPI mirroring commands-api/effects-api registration patterns with viewer-indexed `chartIdsByViewer` and `MapDispatchTest` (10); `FoliaMapBinding` thin override (Bukkit paths inherited; quit/disable hooks delegated); `ChartSpecTokens` (60s TTL, single-use, player-bound, atomic-remove `consume`, sweep seam) with 21-case test; Stage-2 menu wiring (`MenuAction.OpenMap` sealed permit, `/rtp info` row, `BookMenuRenderer` arm, redeem through `MenuRedeemSubcommand`). New baseline `messages.yml::menuInfoBadPointsLabel` propagated via TSV pipeline.
- **[ADR-049](docs/adr/ADR-049-network-mode-platform-neutral-lift.md) authored (Proposed 2026-05-23); supersedes rtp-fabric-ADR-013.** Documentation-only this cycle: re-audit of `rtp-plugin/.../bukkit/network/` showed nine of the thirteen classes (including the 1108-line `NetworkModeBootstrap`) have zero `org.bukkit.*` imports and the four that do touch the Bukkit API touch it only for player join/quit subscription + `Bukkit.getPlayer(uuid)` lookup. ADR-049 adopts a lift-to-`rtp-core` path with a single new SPI primitive (`PlayerLifecycleHook`) on `RTPServerAccessor` rather than the parallel-reimplementation path proposed in rtp-fabric-ADR-013 (which was flipped to Superseded). `MULTI_PLATFORM_PLAN.md` Step J rewritten to reflect the new sub-item list. Implementation is a follow-up change.

### Changed

- **`commands-api` parameter delimiter de-canonicalized; `effects-api` namespaced-id coercion collapsed to a single path.** Follow-up to the `:` -> `=` command syntax migration. `CommandsAPI.parameterDelimiter` / `multiParameterDelimiter` are now `public static final char` (binary-compat for `getstatic`); ~20 internal `TreeCommand` sites use `"="` / `","` literals or `char` overloads; `splitOnParamDelimiter` remains the named primitive. `REQ-API-F-005` re-worded to absolute state. On `effects-api`, `BukkitValueCoercer.resolveViaRegistry`/`resolveSound` lose the `raw.contains(":")` bifurcation in favour of a single `NamespacedKey.fromString` path with bare-token front-loader (preserves `ENTITY_PLAYER_HURT` underscore-to-dot). Legacy `legacyColorFromDelimitedString` removed. Existing `:commands-api:test` (31) and `:effects-api:test` (55) green.

### Fixed

- **/rtp argument-rejection messages now substitute the offending argument, and use the canonical `key=value` syntax** (REQ-RTP-S-004, REQ-RTP-F-013). Four sites in `RTPCmd.compute` (unknown `player`, unknown `world`, unresolvable `region`, region with null world) and two in `ScanResetCmd` / `ScanStartCmd` were calling `langParser.getConfigValue(MessagesKeys.badArg, "player:" + name)` -- the second argument is the parser's *default-when-key-missing*, not a placeholder substitution. With the default `messages.yml::badArg` template `"&c[P0] invalid command argument [arg]"` present in the configuration, the player saw the literal `[arg]` token in the rejection message and never learned which argument was wrong. Fix: each site now fetches with the standard default `"[P0] bad parameter - [arg]"` (matching `BaseRTPCmd.msgBadParameter` and `BukkitBaseRTPCmd.msgBadParameter`) and explicitly calls `msg.replace("[arg]", ...)` to substitute the offending argument. Additionally, all six call sites and the two `MenuRedeemSubcommand` `RunRtpCommand` arms that synthesise `/rtp info <scope:name>` argv now use the canonical `key=value` syntax (`player=<name>` / `world=<name>` / `region=<name>`) rather than the legacy `key:value` form, matching the post-beta.3 commands-api delimiter migration (see *Changed* above) so the same string that the player sees in the rejection is also the string they would type to retry.

- **Network mode: `/rtp region=<self>:<region>` no longer gets stuck on the originating backend** (REQ-RTP-S-004 / REQ-RTP-NET-015). When a player typed an explicit `region=<server>:<region>` whose `<server>` was the local backend's own `serverId`, `NetworkRouter.route(...)` honoured the hint as a hard-pin and funnelled the request through the cross-server gates - it would either bounce the player off-and-back to themselves or REJECT with `REGION_UNAVAILABLE` (depending on what regions the local heartbeat advertised at that instant), instead of just serving the request locally. Fix: at the top of `route(...)`, after normalising the hint, drop it when `hint.equals(localServerId)`. The redundant `<self>:` prefix now coalesces to the equivalent `region=<region>` shape and takes the normal local path under both `LOCAL` and `AUTO` modes. Foreign pins are unaffected (the user's primary use case for the hint). Covered by three new `NetworkRouterTest` cases (`self_pin_coalesces_to_local_under_mode_LOCAL`, `self_pin_coalesces_to_local_under_mode_AUTO`, `foreign_pin_still_routes_cross_server`).

- **Network mode: cross-server `/rtp` arrivals now teleport on Folia** (REQ-RTP-S-004 / REQ-RTP-NET-015, Folia threading). After the prior pin/release fixes finally got the destination backend's `JoinTriggerSource.handleRedeem` to fire, the redeem succeeded and `dispatchRtp` invoked `RTP.scheduler.runTaskLater(hop, 1L)` to hop onto the main thread for `Bukkit.dispatchCommand(player, "rtp")`. On Folia, the bare `runTaskLater` overload lands on the Global Region Scheduler thread, but `CraftServer.dispatchCommand` runs `TickThread.ensureTickThread` against the player's owning region and throws `IllegalStateException: Thread failed main thread check: Dispatching command async` -- so the `/rtp` was thrown away and the player arrived without a teleport. Fix: (a) renamed `RTPScheduler.scheduleTeleport(RTPPlayer, RTPRunnable, long)` to `runTaskForPlayer` -- the name was a historical artifact; the method is a generic "run this on a thread that owns the player after N ticks" primitive used by `TeleportPipelineTask`, `BukkitEffectsHandler`, `AnvilInputSession` and (now) `JoinTriggerSource`; (b) `JoinTriggerSource.dispatchRtp` resolves the arriving `RTPPlayer` and routes the dispatch through `RTP.scheduler.runTaskForPlayer(...)`, which on Folia hops to the player's entity scheduler (the only thread on which `Bukkit.dispatchCommand` passes the tick-thread check) and on Bukkit / Paper / Spigot / Fabric falls through to the existing main-thread path. The legacy `runTaskLater` fallback is preserved for the player-not-resolvable race. Bukkit/Paper/Spigot/Fabric unaffected behaviourally (single-tick-thread platforms).

- **Network mode: cross-server `/rtp` arrivals now teleport for pinned routes too** (REQ-RTP-S-004 / REQ-RTP-NET-015). The Velocity proxy adapter's `onServerPreConnect` handler was eagerly calling `transport.release(token.tokenId(), ReleaseReason.CONSUMED)` immediately after rewriting the connect target. `release.lua` transitions the row to `CONSUMED` AND `DEL`s the `rtp:net:tokactive:<playerId>` active-index key, so by the time the player joined the destination backend ~1-2s later, `JoinTriggerSource.handleLookup` called `findReservation` and got an empty result ("no reservation found"); the redeem path that pins the coord, evicts the local waitlist guard, and dispatches the local `/rtp` therefore never ran. Symptom (after the prior pin and evict-on-redeem fixes had landed): the player would correctly route to the named backend but arrive without any teleport happening. Fix: removed the eager `release(CONSUMED)` from `RtpVelocityPlugin.onServerPreConnect`. The proxy now only uses the reservation to rewrite the connect target; the token remains `CLAIMED` so the destination backend's `JoinTriggerSource` can `redeem(...)` on join (which transitions `CLAIMED -> CONSUMED` and clears the active index in `redeem.lua` atomically with pinning the coord and dispatching `/rtp`). The unknown-target-server `release(BACKEND_REJECTED)` path is preserved unchanged. The 30s reservation TTL plus the existing `ReservationTokenReaper` handle the edge case where the player disconnects mid-transfer and never reaches the backend.

- **Network mode: explicit `/rtp region=<server>:<region>` is no longer silently load-balanced** (REQ-RTP-S-004 / REQ-RTP-NET-015). The proxy SPI `RtpRequest` had no `serverHint` field at all, so the upstream router's hard-pin (whether typed by the player or synthesised by lobby `pickMostKept`) was smuggled by `TransportRequestTriggerSource` into the unrelated `originServerId` slot, which the `BackendSelector` never reads. Every cross-server `/rtp` - pinned or not - therefore went through the load balancer, and an operator who routed a player to a specific backend had no way to make the proxy honour it. Fix: (a) `RtpRequest` gains a dedicated `serverHint` field with a 6-arg back-compat constructor that preserves every existing caller; (b) `TransportRequestTriggerSource.dispatchEnvelope` routes `env.serverHint()` into the new slot and leaves `originServerId` empty (the trigger source is not the request's origin); (c) `DefaultRtpDispatcher.claimAfterSelect` now branches on `request.serverHint()` first: present `=>` `selector.choose(request, snapshot, Optional.of(hint))` and hard-fail with `NO_BACKEND` (rather than silently load-balancing) when the pinned backend is not eligible, bypassing the region-aware fallback entirely; (d) waitlist round-trip preserves the hint - `DefaultRtpDispatcher.parkOnWaitlist` writes `request.serverHint()` into the `WaitEnvelope` and `NetworkWaitlistDrainer.redispatch` reads it back. Covered by two new cases in `DefaultRtpDispatcherTest` (`serverHint_is_honoured_and_passed_to_selector_filter`, `serverHint_pin_unreachable_hard_fails_NO_BACKEND`).
- **Network mode: cross-server arrivals now actually teleport** (REQ-RTP-S-004 / REQ-RTP-NET-015). After `JoinTriggerSource` successfully redeemed a cross-server `ReservationToken`, the destination backend re-dispatched `/rtp` for the arriving player; that `/rtp` was then short-circuited by `NetworkWaitlistGuard` because the lobby-seeded `NetworkStatusCache` row was still non-terminal (`QUEUED` / `RESERVED` / `TRANSFERRING`) and would not be reconciled until the next supplier poll. Net symptom: player hops servers, sees no teleport, no warning logged. Fix: `JoinTriggerSource.onRedeemed` now evicts the arriving player's row from `NetworkStatusCache` via the new `NetworkStatusCache.evictLocal(UUID)` before dispatching `/rtp`, so the waitlist guard sees the player as no longer non-terminal and the local pipeline polls the pinned redeemed coord. The eviction is a local-state correction only; the proxy remains the source of truth for the authoritative terminal transition, which `pollOnce` will observe on its own schedule. Wired via a new `JoinTriggerSource(transport, serverId, statusCache)` constructor overload plus a late-bind `setStatusCache(...)` setter so the trigger source can be constructed before the status cache in the bootstrap sequence. Covered by two new cases in `NetworkStatusCacheTest` (`evictLocal_removes_row_without_firing_terminal_listener`, `evictLocal_also_clears_sticky_seed`). **Note:** an immediate side effect of the load-balancing fix above is that pinned cross-server `/rtp` finally lands the player on the *named* backend, which is the one whose `JoinTriggerSource` will see the matching `ReservationToken` - so the prior beta.4 evict-on-redeem fix is now actually exercised on the destination backend in the pinned scenario.
- **Permission-granted on-event teleports and effects now fire on Fabric** (rtp-fabric-ADR-011). `RTPCommandSender#getEffectivePermissions()` previously returned `Collections.emptySet()` in all three Fabric player variants and the console sender because `fabric-permissions-api` is a check-only SPI; on-event grants and effect dispatch consequently fell through to `config.yml` defaults on Fabric. New enumerator threads node-set discovery through LP-Fabric when present (with op closed-namespace fallback) so `rtp.onevent.*` and effect tokens resolve identically to Bukkit. Covered by `FabricEffectivePermissionsTest` (22).
- **YAML database race in `YamlFileDatabase.setValue(String, Map)`.** Override no longer delegates to `super.setValue` (which enqueues async via `getTable().thenAccept` and races the re-nested enqueue, flattening top-level YAML keys); the `localTables` snapshot is now updated inline with the re-nested shape. Flake in `YamlFileDatabaseTest.setValue_flatRow_isNestedUnderPrimaryKey` resolved; `:rtp-core:test` 2460/2460 PASSED.
- **Lobby no-arg `/rtp` now spreads across ready peers instead of always picking the lex-smallest backend.** Three interacting defects on the no-arg lobby dispatch: (a) `BukkitBackendStateSampler.sample()` was using the 13-arg `BackendHeartbeat` ctor that defaults `keptCount=0` and `regionKeptCounts=Map.of()`, so every peer scored 0 and the deterministic lex tie-breaker won; (b) peers not accepting requests were not excluded from `pickMostKept`; (c) local recordDispatch decrement was not applied. Covered by new `pickMostKept_*` regressions and 18/18 in `LobbyModeTest`.
- **`safety.yml::unsafeBlocks` and probed block names accept either bare or `minecraft:`-namespaced form, with modded namespaces preserved verbatim.** Direct analog of the biome fix below for future-proofing against an eventual Keyed-registry `Material` release (S-001 territory: missing block matches would drop the unsafe-block guard). New `MaterialNames#matches(Set<String>, String)` helper, sibling of `BiomeNames`; `PregenTask` and `ScanTask` probe-block checks route through it. Covered by `MaterialNamesTest` (22).
- **Biome arguments and `safety.yml::biomes` accept either bare or `minecraft:`-namespaced form across Bukkit-family platforms.** Follow-up to the `:` -> `=` command syntax migration. New `BiomeNames#matches` helper treats the uppercase vanilla `MINECRAFT:` prefix as optional while preserving modded namespaces (`IRIS:`, `TERRALITH:`); `RTPCmd` no longer strips namespaces on input. All four `ScanTask` and both `PregenTask` biome-filter sites route through the helper. Covered by `BiomeNamesTest` (22); `ScanTaskProcessingTest` and `ParameterParsingTest` confirm no regression.

### Security

- **HMAC envelope on the Redis transport wire** (rtp-proxy-ADR-010 Phase 2e-Redis A3). Every published `BackendHeartbeat` / `ProxyHeartbeat` row carries an `hmac=<hex>` field signed via `HmacVerifier` over the canonical line-oriented payload; verification on read drops tampered or legacy-NULL rows with REQ-RTP-S-004 WARNING. `NetworkBindings.open` constructs `HmacVerifier.loadFromEnv` and degrades to InMemory on loader failure. `rtp test network all` audits the integration end-to-end.
- **HMAC envelope parity on the SQL transport** (rtp-proxy-ADR-010). `SqlNetworkStateBinding` signs every published heartbeat + `ReservationToken` row over the same canonical field-order strings as Redis, verifies on every read site with constant-time compare, and drops tampered/legacy-NULL `hmac` rows. `SqlNetworkStateSchema` adds an idempotent `ALTER TABLE ... ADD COLUMN IF NOT EXISTS hmac VARCHAR(128)` migration that tolerates the MySQL/MariaDB dialect quirk (portable form first, bare `ADD COLUMN` retry, SQLSTATE 42S21/42xxx swallowed). Pre-A3 SQL deployments self-heal on the next heartbeat tick; verifier-null ctor preserved for back-compat.

---

## [3.0.0-beta.3] - 2026-05-20

### Changed

- **User-facing color palette pulled away from yellow/gold/light tones, with occasional custom hex accents.** Menu titles (`menuRootTitle`, `menuAdminPanelTitle`, `configSelectorTitle`, `configFileTitle`, `configTypePickerTitle`, `configSubParamPageTitle`) lose `&6&l` (bold gold) and adopt `#5FB3B3&l` (muted teal) for general titles or `#9D7CD8&l` (lavender) for the admin panel; the admin-panel front-page entry row's `&6` arrow becomes `#9D7CD8`; admin-panel row arrows shift from cyan `&b` to muted teal `#5FB3B3` (destructive `Reload` / `Full diagnostics` rows keep `&c`); config selector and per-key rows lose `&e[key]` / `&f[value]` for `#7E9CC8[key]` / `#B084DC[value]` (muted blue / muted purple). System status messages (`alreadyTeleporting`, `teleportDeniedReloading`, `chunkLoading`, `teleportCancel`, `unsafe`, `cooldownMessage`, `notEnoughMoney`, `invalidWorld`, `noPerms`, `locationSaved`, `locationLoaded`, `scanReset`, `scanCancel`) drop yellow `&e` for neutral `&7`; `delayMessage` adopts `#9D7CD8` lavender and `teleportMessage` adopts `#5FB3B3` teal for occasional accent. `/rtp info` value placeholders switch from bright white `&f` to muted blue `#7E9CC8`. `PLAYER_COOLDOWN` PAPI status moves from `&e` to `#9D7CD8`. Propagated through every shipped locale via the TSV pipeline (new `scripts/recolor-locales.ps1` plus `locale-files-from-csv.ps1`). Verified by `LocaleParityTest` (154/154) and full `gradlew build`.

- **`Ellipse` shape parameterised over a dedicated `EllipseMemoryShapeParams` enum, with elliptical inner exclusion and Rectangle-style rotation (amends [ADR-034](docs/adr/ADR-034-memory-shape-catalog.md)).** The second semi-axis `radius2` previously lived on the shared `GenericMemoryShapeParams` enum, which forced every single-radius shape (`Circle`, `Square`, and their `_Normal` variants) to carry a meaningless `radius2` default solely to satisfy `Shape`'s "every enum constant present" constructor invariant. `radius2` is now hosted on the new `EllipseMemoryShapeParams` enum alongside the other ellipse knobs (`mode`, `radius`, `centerRadius`, `centerRadius2`, `rotation`, `centerX`, `centerZ`, `weight`, `uniquePlacements`, `expand`), and `Ellipse` extends `MemoryShape<EllipseMemoryShapeParams>` directly rather than `Circle`. `Ellipse` additionally gains two new knobs: `centerRadius2` (the second axis of an elliptical — rather than circular — inner exclusion) and `rotation` (degrees, mirroring `RectangleParams.rotation`, applied around (`centerX`, `centerZ`)). The spiral 1D mapping (`getRange`, `rand`, `xzToLocation`, `locationToXZ`, `neighbourRingOffset`) continues to use the wider of each radius pair as the bounding circle; `contains` enforces the true rotated elliptical inner / outer shells. Internal refactor — `rtp-api` is unaffected.

- **`rtp.personalqueue` permission semantics decomposed (`ADR-043`).** Retires the bundled `RegionQueueManager.queue(UUID)` / `Region.queue(UUID)` method and replaces it with three named entry points: `openPersonalQueue(UUID)` (per-uuid coordinate bucket opt-in, schedules a single pregen fill), `closePersonalQueue(UUID)` (returns banked coordinates to L2 `unkeptLocations`), and `requestTeleport(UUID)` (enrolls the player on the teleport waitlist). Join / respawn / world-change listeners now open the bucket without enqueuing a teleport -- fixes the "ops appear in the queue after every join" symptom. Quit closes the bucket so coordinates are not stranded across sessions. **Breaking for addons** that called `Region.queue(UUID)` or `RegionQueueManager.queue(UUID)`: pick `openPersonalQueue` (bucket opt-in) or `requestTeleport` (waitlist enrol) per the new semantics. Covered by `ReqRtpAdr043PersonalQueueSeparationTest` (6 cases).

### Added

- **`MemoryShape.chunkToLocations(int cx, int cz)` and `MemoryShape.addBadChunk(long location)`** -- new bounded inverse of `xzToLocation` at chunk granularity, plus a sibling of `addBadLocation` that marks every 1D index decoding to the same chunk. The Archimedean-spiral parameterisation in chunk units ([ADR-001](docs/adr/ADR-001-archimedean-spiral-1d-mapping.md)) guarantees <= 2 preimages per chunk, so `chunkToLocations` is O(1) (angular walk plus a shape-specific radial probe via a new `neighbourRingOffset` hook on `Circle` / `Square`). `addBadChunk` is intended for chunk-attributable rejections (biome, claim, border, anvil-prefilter, ocean), is idempotent, falls back to `addBadLocation` on outside-shape slop, and returns the count newly marked (0-2). All production rejection call sites in `ScanTask`, `PregenTask`, `Polygon`, `ChunkyRTPShape`, and the `uniqueplacements`-knob `rand()` paths of `Circle` / `Square` / `Rectangle` / `Circle_Normal` / `Square_Normal` are migrated to `addBadChunk`; within a chunk the per-column selection order is deterministic, so re-rolling the same chunk would re-pick the same column and reject identically. The only surviving `addBadLocation` calls in production are the internal fallbacks inside `addBadChunk` itself and the public API method body. Covered by `MemoryShapeChunkToLocationsTest` (10 cases).
- **`backlogRefillThreshold` performance knob** -- new double-valued setting in `performance.yml` (range `[0.0, 1.0]`, default `0.5`) that adds hysteresis to the L3 backlog cache ([ADR-028](docs/adr/ADR-028-l3-backlog-cache.md)) refill loop. Previously every `Region.execute()` pulse re-topped `backlogLocations` to capacity, scattering shape picks across many `.mca` files and weakening the cross-region amortization of the world-shared anvil bin index. With the knob, the refill latch deactivates at `capacity` and re-arms only when size falls below `floor(threshold * capacity)`, concentrating shape picks (and their anvil verifications) into bursts that share `.mca` files for better CPU and memory locality. Edge cases: `0.0` is opt-out (never re-arms), `1.0` reproduces the prior always-refill behaviour; out-of-range clamped, parser failures fall back to default. Covered by `BacklogRefillThresholdTest` (7 cases).
- **In-house YAML substrate (`io.github.dailystruggle.rtp.common.configuration.yaml`) now replaces `simpleyaml` across `rtp-core`** ([ADR-025](docs/adr/ADR-025-replace-simpleyaml-with-in-house-substrate.md), 2026-05-15 revision; [ADR-042](docs/adr/ADR-042-block-only-comment-preservation.md)). Hand-rolled, zero-dependency reader / writer / AST (`RtpYamlConfig`, `RtpYamlSection`, `RtpYamlMapping`, `RtpYamlSequence`, `RtpYamlScalar`, `RtpYamlReader`, `RtpYamlWriter`) with block-comment preservation, atomic-rename `save()`, and a simpleyaml-shaped parity surface (typed getters `getString`/`getInt`/`getLong`/`getDouble`/`getBoolean`/`getList`/`getStringList`, predicates `isSet`/`isConfigurationSection`/`isString`/`isInt`/`isBoolean`/`isList`, `getValues(boolean deep)` / `getMapValues(boolean)`, `getName`/`getCurrentPath`, `getComment`/`setComment`, `remove(String)`, `createSection(String[, Map])`, `loadWithComments`/`saveWithComments`, `loadConfiguration(InputStream, boolean)`, `createOrLoad()`, `exists`/`getFilePath`/`createNewFile[(boolean)]`/`copyTo(String|File)`, plus `RtpYamlOptions` exposing `copyDefaults(boolean)` and `indent(int)` and a `RtpYamlConfig.addDefault(String, Object)` set-if-absent helper). All `rtp-core` production and test call sites — `ConfigParser`, `MultiConfigParser`, `LanguageBootstrap`, `YamlFileDatabase`, `FactoryValue`, `RTP`, `RegionConfigLoader`, `SubConfigCmd`, `LanguageCmd`, `ListCmd` / `ListAddParameter` / `ListRemoveParameter`, and the six corresponding test files — now use the substrate directly; the `me.carleslc.Simple-YAML` Gradle dependency and `rtp-plugin` shadow-relocate rules will be dropped in the next beta cycle once `rtp-core` ships green end-to-end across all platforms. Covered by `RtpYamlSimpleYamlParityTest` (21 cases) on top of the substrate's read/write/comment suites (211 + 177 + 177 cases), with the full `rtp-core` test pass continuing to validate every migrated call site.
- **Menu framework foundations in `rtp-api`** ([ADR-035](docs/adr/ADR-035-interactive-menus-book-first.md) amended 2026-05-15; [ADR-044](docs/adr/ADR-044-command-tree-menu-reflector.md)). New `io.github.dailystruggle.rtp.api.menu` package introduces the platform-neutral menu surface that the upcoming `/rtp menu` consumer (with `/rtp config` as the first concrete browser) and downstream renderers will build against: immutable `MenuModel` / `MenuPage` / `MenuLine` / `MenuFragment` records, a sealed `MenuAction` (`RunRtpCommand` with defensive-copy `args`, `ChangePage`, `SuggestInput`, `OpenExternalUrl`), the `MenuRenderer` interface, the `MenuTokenRegistry` single-use / TTL / per-player-bounded contract, and the ADR-044 reflector seams `MenuConsumerProfile` (with a trivial `defaultProfile()` factory) + `YamlCommentLookup` (with an `EMPTY` constant for non-YAML-backed consumers). No platform imports, no Adventure, no `rtp-core` dependency — addons can build menus against this surface today. Concrete `LocalMenuTokenRegistry`, `MenuRedeemSubcommand`, `CommandTreeMenuBuilder`, and the Paper/Folia `BookMenuRenderer` ship in subsequent commits within the same beta.3 cycle. Covered by `MenuModelSurfaceTest` (12 cases). The matching 
tp-core concretes for the menu framework — LocalMenuTokenRegistry, MenuRedeemSubcommand, CommandTreeMenuBuilder, and ConfigMenuConsumerProfile — now ship alongside the API surface, gated by five new MessagesKeys entries (menuInvalid, menuExpired, menuUnknownPlayer, menuHoverFallbackType, menuHoverFallbackBounds) so user-facing rejection strings and hover fallbacks remain configurable per REQ-RTP-F-013. The redeem subcommand atomic-consumes tokens (owner-keyed CAS), rejects foreign UUIDs, and dispatches RunRtpCommand actions through the live /rtp tree; the reflector hides permission-denied nodes (scope answer A) and emits Run-for-subcommand / Suggest-for-parameter fragments with a YAML-comment → typed-fallback → null hover chain. messages.yml/TRACEABILITY.md wiring and the actual /rtp menu command registration land in Stage 3 of the same beta.3 cycle. Covered by MenuStageTwoTest (12 cases).
- **Curated front page for `/rtp menu` (Stage B).** When a player runs `/rtp menu` with no arguments, the root page is now a hand-picked landing screen rather than the flat alphabetised reflection of the `/rtp` command tree. The new `FrontPageBuilder` (in `rtp-core`) emits two permission-gated row catalogues: a player view (Teleport / Region picker / Biome picker / Help, picker rows appearing only when the corresponding root parameter is registered and has at least one suggestable value) and an admin view gated by `rtp.menu.admin` that collapses to a single `Admin panel` entry row leading into a dedicated curated admin-panel page (PROPOSAL-admin-panel.md v2; see below). Descending into any subcommand falls through to the existing `CommandTreeMenuBuilder` reflector. Configurable `MessagesKeys` entries (`menuFrontPageSection*`, `menuFrontPageRow*`, `menuFrontPageHover*`) keep every label and hover under operator control per REQ-RTP-F-013. Covered by `FrontPageBuilderTest` (7 cases).
- **Curated admin panel for `/rtp menu` (PROPOSAL-admin-panel.md v2).** The single `Admin panel` entry row on the front page (admin viewers only, gated by `rtp.menu.admin`) opens a dedicated curated `MenuModel` built by the new `AdminPanelBuilder` (`rtp-core`). The panel organises operator tooling into four self-hiding sections: Configuration (`Config editor` -> `OpenConfigSelector`, gated on `rtp.config.view`), Diagnostics (`Server info`, `Full diagnostics`, `Memory tracker snapshot` when `/rtp test memory` is registered, `Scan control` gated on `rtp.scan`), Lifecycle (`Reload` gated on `rtp.reload`, with a warning hover), and Browse (`Browse all commands` -> reflected-tree fallback). A `Back` row at the bottom emits the new `MenuAction.OpenFrontPage` token to return to the curated front page. Section dividers auto-suppress when their entire body is hidden, so a partial-admin viewer never sees an orphan header. Two new no-payload sealed `MenuAction` variants (`OpenAdminPanel`, `OpenFrontPage`) extend the menu surface to 13 permitted types; both round-trip through `/rtp menu token:<t>` per ADR-035. New `MenuRedeemSubcommand.MenuCuratedPageBuilder` SAM hosts the two dispatch arms (`dispatchOpenAdminPanel` gated on `rtp.menu.admin`, `dispatchOpenFrontPage` ungated). Seventeen new `MessagesKeys` entries (`menuFrontPageRowAdmin` + hover, `menuAdminPanelTitle` / `menuAdminPanelHint`, four section dividers, ten row+hover pairs, one back row); eight prior front-page-inline keys (`menuFrontPageRow{Info,Config,Scan,Diagnostics,Reload}`, `menuFrontPageHover{Diagnostics,Reload}`, `menuFrontPageSectionAdmin`) are retired since their content moved to the panel. Covered by `AdminPanelBuilderTest` (12 cases) and two new `BookMenuRendererTest` round-trips.
- **`maps-api` documentation surface ([ADR-046](docs/adr/ADR-046-maps-api-module.md), [maps-api-ADR-001](maps-api/docs/adr/maps-api-ADR-001-bootstrap.md), 2026-05-16).** Authored the documentation layer of the runtime-cartography subsystem ahead of any code landing: five new functional requirements `REQ-RTP-MAP-001..005` in [`REQUIREMENTS.md`](docs/dev/REQUIREMENTS.md) §1.7 (require-by-contract `MapBinding` extending REQ-RTP-S-006; non-blocking `ChartRenderer` extending REQ-RTP-F-008 / REQ-RTP-S-005; `MemoryTracker` lifecycle release on cancel / disconnect / disable; Lite-only `NoopMapBinding`; in-house Mermaid subset with no external runtime, scripting engine, or third-party graph-layout dependency), matching rows in [`TRACEABILITY.md`](docs/dev/TRACEABILITY.md), and the in-module bootstrap ADR [`maps-api-ADR-001`](maps-api/docs/adr/maps-api-ADR-001-bootstrap.md) locking package layout (`mapsapi` root + `model` / `render` / `render.mermaid` / `noop` / `bukkit` / `fabric` subpackages, with `bukkit` the only subpackage allowed to import `org.bukkit.*`), the *logical-palette-now, concrete-table-Stage-2* palette policy (32-symbol `PaletteIndex` contract, vanilla map-palette bytes carried per-binding), and the assembly-level Lite trim posture (shadow-jar exclusions in `rtp-plugin`, audited by an extended `liteJarStructureCheck` clause in Stage 2). Stage-1 SPI skeleton now also landed (2026-05-16, same beta.3 cycle): new `maps-api` Gradle module (`settings.gradle`, `maps-api/build.gradle` `java-library` + ArchUnit) with the platform-neutral SPI surface — `MapCanvas` (128x128, logical-palette bytes), `MapHandle` / `MapAllocationRequest` (record + `Locking` enum) / `Cancellation`, `MapBinding` (allocate / renderEphemeral / bindLive), sealed `ChartModel` (`Heatmap2D` / `CategoryDistribution` / `TimeSeries` / `RegionCoverage` / `MermaidChart`, all defensively copied), `ChartRenderer` and the first concrete `HeatmapRenderer`, the `NoopMapBinding` (REQ-RTP-MAP-001 / REQ-RTP-MAP-004 default — every entry-point throws `IllegalStateException` with the documented prefix), four `package-info.java` cross-references — plus the `InMemoryMapBinding` test double and four traceable tests: `ReqRtpMap001RequireByContractTest`, `ReqRtpMap002NoChunkIoTest` (ArchUnit + manual blocking-future-call scan over `mapsapi.render`), `HeatmapRendererTest`, `MapsApiSurfaceTest`. 25/25 maps-api tests green; `.\gradlew build` green across every module.

### Fixed

- **Occasional mid-air placement at an invalid Y from L3 backlog entries** (S-001 regression closed). The L3 backlog refill path ([ADR-028](docs/adr/ADR-028-l3-backlog-cache.md)) stamps a placeholder Y on each unverified entry *before* any ground check has run, and the L2→L1 promotion verification in `Region` previously trusted that Y and only re-tested the single feet-block via `RTPChunk.isSafe(localX, y, localZ, ...)`. When the placeholder happened to land in air (the prior implementation used the vert window's midpoint), the single-block check returned safe and the player was teleported mid-air. The promotion verifier now re-runs the region's `VerticalAdjustor.adjust(chunk)` against the loaded chunk and uses its returned `RTPCoords` as the player-facing coordinate — the same ground+headroom sweep the live-spiral path already enforces — so no separate ground predicate has to be kept in sync. Verification still fails closed: any exception or null result drops the entry and purges the DB row. The placeholder Y used during L3 enqueue (where the value never reaches a player and only feeds biome/border probes in `PregenTask` / `ScanTask`) is also changed from the vert-window midpoint to the adjustor floor clamped to `[world.minHeight, world.maxHeight - 1]`, so even if a future consumer accidentally trusts it the value is at least inside the searchable window. Regression guarded by `RegionPipelineTest`.

- **`effects-api` Folia dispatch hardening for `PotionEffect` and `FireworkEffect`** (closes the "firework never spawns / `IllegalStateException: Asynchronous effect add`" symptom on Folia). Both effects now resolve the `Plugin` caller through `EffectsAPI.getInstance()` with a graceful S-006 fallback: if `init(Plugin)` hasn't run, `caller` degrades to `null` and the dispatcher seam (`PotionEffect.entityDispatcher` / `FireworkEffect.regionDispatcher`) is still consulted so the Folia `EntityScheduler` / `RegionScheduler` path is taken when available. Removed an `effects-api`-layer call to `Bukkit.getPluginManager().getPlugin("RTP")` that named the host plugin and violated the platform-agnostic boundary of the module; the legacy primary-thread fallback already handles the null-caller case via `safeRun(apply)`. `PotionEffect.applyOnEntityThread` additionally swallows `addPotionEffect` failures (`IllegalStateException: Asynchronous effect add`, invalid effect data) via `safeRun` so a misrouted effect never propagates back through the teleport caller. Regression guarded by `FoliaDispatchTest` (4 cases).

- **Locale TSV pipeline corruption in `messages.yml::placeholders[0]`** (closes a recurring pipeline-roundtrip bug). Six locales (`es`, `ja`, `ko`, `pt`, `ru`, `zh`) had accumulated multi-megabyte runs of stacked backslashes plus trailing YAML residue (`" #P0`) in the `placeholders` list-head value as the result of repeated `locale-files-to-csv` / `locale-files-from-csv` passes mis-handling a value that contained both YAML special characters and an inline comment. Affected locale TSVs were repaired in place to the baseline value `[RTP]` and the regenerated `lang/<loc>/messages.yml` files now match the rest of the locale set. The repair was applied in-place to the affected TSVs and YAMLs; no source-tree artifacts remain.

- **Three universally-untranslated `config.yml` comment blocks translated into all shipped locales** (`database.port`, `network.redis.enabled`, `network.redis.port`). These keys were added to baseline in a recent reconcile pass and seeded into every locale TSV from English; no locale maintainer had yet translated them, so every non-English server displayed English Redis/database documentation. Translations now ship for `de`, `fr`, `es`, `it`, `nl`, `pt`, `ja`, `ko`, `zh`, `ru`, `pl` (new), and `cat` (Internet Cat dialect: see *Domain Analogies & Aliases* in `.junie/AGENTS.md` clarifying that `lang/cat/` is the cat-themed locale, not Catalan; Catalan would be ISO `ca`).

- **Four new locale trees shipped since beta.2: Italian (`it`), Polish (`pl`), Portuguese (`pt`), and Russian (`ru`).** New `lang/<loc>/` trees per [`TRANSLATION_GUIDE.md`](docs/dev/TRANSLATION_GUIDE.md) §8 (first-pass / known-stale locales seeded from English baseline under translated keys, ready for native-speaker fill-in). The three new Redis/database comments above are translated into each; broader recurring English-comment translations across all 12 locales are described in the bullet below. All four pass `LocaleParityTest`. Locale set is now 12: `cat`, `de`, `es`, `fr`, `it`, `ja`, `ko`, `nl`, `pl`, `pt`, `ru`, `zh`.

- **Recurring high-multiplier English comment leaks translated across every shipped locale.** A scan of `preceding_comment` cells in `scripts/out/locale-<loc>.tsv` identified 27 baseline `(file, key)` signatures whose comment block was still untranslated in 10-12 locales each (i.e. nearly every shipped locale missed the same row), affecting in-game runtime config descriptions and `/rtp info` message documentation. Translated comments now ship for all affected locales (`cat`, `de`, `es`, `fr`, `it`, `ja`, `ko`, `nl`, `pl`, `pt`, `ru`, `zh`) for the following signatures: `config.yml` (`database.password`, `database.name`, `database.username`, `playerCommands`, `menu`); `messages.yml` (`badArg`, `consoleCmdNotAllowed`, `cooldownMessage`, `invalidWorld`, `newWorld`, `reloading`, `unsafe`, `notEnoughMoney`, `noLocationsQueued`, `noPerms`, `queueUpdate`, `teleportDeniedReloading`, `infoDatabaseLatencyMs`, `infoHealthPipelineHeader`, `infoMSPTLive`, `infoTps`, `menuFrontPageHoverDiagnostics`, `menuFrontPageHoverReload`, `menuUnknownPlayer`); `integrations.yml::rerollFactions`; `metrics.yml` (`foliaAggregationMspt`, `foliaAggregationTickBudget`, `foliaAggregationTps`, `foliaIncludeRegions`); `performance.yml` (`loginCacheEnabled`, `loginCacheCap`). Doc-tag lines (`@type:`, `@range:`, `@default:`, `@unit:`, `@options:`, `@source:`) are preserved verbatim per the in-game runtime-config description contract; only the prose lines that precede them were translated. `network.redis.password` (which shares `base_key` with `database.password`) was correctly preserved by disambiguating via canonical baseline parent path. ~351 TSV rows patched, regenerated YAMLs verified via `LocaleParityTest` and full `gradlew build`.

- **New auditing tooling.** Added `scripts/scan-locale-comment-leaks.ps1` (per-locale English-leak scanner with `@type:`/`@range:`/etc. doc-tag stripping per the in-game runtime-config description contract) and `scripts/list-common-comment-leaks.ps1` (cross-locale "translate once, apply everywhere" identifier sorted by locale-multiplier). Both honor the rule "ignore the values after `@` symbols, but keep the prose": doc-tag lines are stripped before judging residual prose for English connector words, so a comment like `# Database / schema name. Used by mysql/postgresql only.\n# @type: string` is scanned only on the first line (the in-game description) and not falsely cleared by the `@type:` tail.

- **REQ-* and ADR-* references scrubbed from all shipped config-file comments.** Comments in `rtp-plugin/src/main/resources/*.yml` (baseline) and every `lang/<loc>/*.yml` previously referenced internal traceability identifiers (e.g. `See ADR-015.`, `(ADR-035 / ADR-044)`, `REQ-RTP-F-013: ...`, `commands-api-ADR-001`, `effects-api-ADR-005`, `effects-api-ADR-001`) intended as developer-side cross-links to `docs/adr/` and `docs/dev/REQUIREMENTS.md`. These identifiers are noise to operators reading the shipped YAML and have been removed from baseline files (`config.yml`, `language.yml`, `messages.yml`, `performance.yml`, `plugin.yml`, `safety.yml`, `regions/default.yml`, `effects/default.yml`) and propagated through the locale TSV pipeline so every `lang/<loc>/` mirror is also stripped. Cross-references remain intact in `docs/`, ADRs, and code-side Javadoc / commit messages.

- **Missing baseline doc-tag lines (@type:, @range:, @default:, @unit:, @options:, @source:) restored on translated comment blocks across every shipped locale, and the previously-English # Note: #minecraft:tall_flowers is an item-only tag in vanilla block in `safety.yml` translated.** When prior locale-maintainers translated only the prose lines of a comment, they sometimes dropped the trailing doc-tag line that the in-game runtime-config description contract relies on; the canonical baseline doc-tag string is language-agnostic and copyable verbatim, so the locale TSV pass now appends any baseline doc-tag line that is missing from the locale's `preceding_comment`. Doc-tag values after the `@` are preserved unchanged; the prose lines that precede them were translated for the `tall_flowers` block (11 locales patched; Italian already had the translation). 30-36 doc-tag-only patches per locale; regenerated YAMLs verified via `LocaleParityTest` and full `gradlew build`.


- **Chinese (`zh`) locale: 49 untranslated key names in `messages.lang.yml` translated.** New `info*` health/metrics block keys (`infoQueueDepth`, `infoPendingTeleports`, `infoAvgPipelineMs`, `infoHeap`, `infoHealthPipelineHeader`, `infoTps`, `infoMSPTLive`, `infoSoftCap`, `infoDatabaseLatencyMs`, the six `infoThreshold*Green`/`Yellow` rows, `infoThresholdNetworkAgeYellow`, `infoFoliaRegionsHeader`, `infoFoliaRegion`) plus the entire `menu*` block (27 keys: `menuInvalid` / `menuExpired` / `menuUnknownPlayer` / hover/back/execute/pick/type/constructed/root/page-prev/next / front-page section + row + hover entries) and three stragglers (`busy`, `invalidCommand`, `version`) were shipping as English identity placeholders. Translated to descriptive Chinese names matching the existing `信息_*` / `菜单_*` naming pattern.

- **Inline/trailing comments baked into quoted YAML scalar values cleaned up across 6 locales (`es`, `ja`, `ko`, `pt`, `ru`, `zh`).** A prior pipeline pass had collapsed leading-position comment blocks into the value column for rows like `pregeneratedPreference: "0.0 # Probability ..."` and `evento_cambioMundo: "true # Cuando el jugador cambia de mundo"` (~44 rows in `performance.yml` and `logging.yml`). Fix: trailing `# ...` text stripped from the value; locale-translated inline text promoted to the row's `preceding_comment` column (so it renders as a leading description above the key, matching baseline shape and feeding the in-game runtime-config description contract); rows whose inline text was still English (`performance.yml`: `pregeneratedPreference`, `backlogRefillThreshold`) had `preceding_comment` restored from baseline verbatim. Regenerated YAML tree verified via `LocaleParityTest` + full `gradlew build`.
---

## [3.0.0-beta.2] - 2026-05-12

### Added

- **L3 backlog cache (`ADR-028`) is now implemented** ([ADR-028](docs/adr/ADR-028-l3-backlog-cache.md), Accepted 2026-05-07). New optional `RegionQueueManager.backlogLocations` (`BacklogLocationBuffer`) sits upstream of L2 `unkeptLocations`: spiral picks land as unverified entries (no chunk I/O, no DB row), each `Region.execute()` pulse anvil-verifies one `.mca` bin (32×32 chunks; cross-RTP-region amortized via a world-shared weak-ref bin index), and head-contiguous `VALIDATED` entries promote into L2. Head-blocking on `UNVERIFIED` preserves spiral order; `INVALIDATED` heads drop without stalling. New per-region `backlogCacheCap` knob in `regions/<region>.yml` (default `1000` on the main jar; lite omits the key so the in-code fallback resolves to `0` ⇒ L3 disabled). Not persisted to the DB by design — restart re-selects fresh entries. Covered by `BacklogLocationBufferTest` and `WorldBacklogBinIndexTest` (21 cases).
- **Initial Fabric (`rtp-fabric`) platform support for Minecraft 1.21.x.** Beta.1 shipped a non-functional Fabric source tree marked out of scope; beta.2 turns it into a working adapter with `/rtp` teleporting end-to-end: async chunk path (no synchronous main-thread loads), Brigadier command tree (`RTPCmdFabricRoot`) with parameter and subcommand parity matching Bukkit (5 parameters, 5 of 6 subcommands), tab-complete at the `/rtp` root, anvil pre-filter parity, multi-version support (1.20.x / 1.21.x including 1.21.11), and permissions via `fabric-permissions-api` with an `ops.json` fallback. Covered by `rtp-fabric-ADR-001` through `rtp-fabric-ADR-007`. **Known Fabric limitations** (see *Known Issues* below): permission-based effects do not fire, and SQL database backends are not wired (SQLite fallback only; MySQL/PostgreSQL/H2 are unplanned on Fabric). Deferred to a later release: `TestCmd` lift, event-bus wiring, online-player tab-complete.
- **`GlideEffect` in `effects-api`** (effects-api-ADR-001) — folds `addons/RTP_Glide` into `effects-api` as a first-class effect with a shutdown-time safe-landing fallback and a new `PlayerLandEvent.Reason`.
- **`FixedAdjustor` vertical adjustor (`vert: FIXED`).** Third engine alongside `LINEAR` / `JUMP`; places the player at a single configured Y in mid-air for skyblock-style worlds (requires the platform tool). Covered by `FixedAdjustorTest`.
- **`pregeneratedPreference` performance knob** — new double-valued setting in `performance.yml` (range `[0.0, 1.0]`, default `0.0`) that probabilistically rejects candidate chunks whose column has not yet been generated on disk, biasing the location selector toward pregenerated terrain while still allowing fallback to ungenerated land. The same weighted gate is applied at the two coordinate-generation sites that feed the caches: the L1 live spiral inside `PregenTask.runAttempt()` (upstream of the anvil pre-filter), and the L3 backlog refill inside `Region.processBacklog()` Step 1 (right after `Shape.select()`, before the unverified entry is enqueued). The check is rejection sampling -- a rejected pick simply advances to the next shape draw, so no validation effort is wasted on locations that the operator's `pregeneratedPreference` would later drop. Both paths share `Region.pregenPrefRejects()` and use the existing non-blocking `RTPWorld.isChunkGenerated` probe (S-005 safe -- no chunk loads triggered). Dropped candidates on the L1 spiral path are attributed to the new `LocationGenerator.FailTypes.ungenerated` bucket for `/rtp info` and `rtp test full` auditing; L3 refill rejections are silent (the candidate never entered any cache). Default `0.0` preserves prior behavior; `1.0` strictly avoids ungenerated chunks. The L3 refill loop additionally caps *consecutive* rejections per pulse at `max(16, backlogCapacity)`, so a high-weight setting on a sparsely-pregenerated world cannot turn rejection sampling into an unbounded spin -- once the cap trips, the refill yields the remainder of the pulse to the deficit loop and other consumers and retries on the next pulse. The L1 spiral path is bounded by the existing `maxAttempts` ceiling (each rejected pick consumes one attempt). Covered by `PregeneratedPreferenceTest` (8 cases including a bounded-refill regression guard).
- **Declarative effect-group configuration via `effects/<group>.yml`** (effects-api-ADR-005). Each file under `<pluginDir>/effects/` declares a named group with fields `when:` (pipeline stage), optional `permission:` / `players:` / `inherit:`, and a required `effects:` token list (same grammar as `rtp.effect.<stage>.*` permission suffixes). Loaded via the existing `MultiConfigParser<EffectsGroupKeys>` plumbing — no bespoke loader class — so per-locale `effects.lang.yml` field-key remap and `/rtp reload` hot-swap come for free. The plugin now registers an `effects/` parser in `Configs.reloadConfigs()`; `BukkitEffectsHandler` and `FabricEffectsHandler` re-resolve groups per teleport via a new `EffectsResolver.resolveUnioned(...)` helper that unions permission-derived nodes with config-driven tokens before feeding `EffectFactory.buildEffects(prefix, ...)`. Fabric admins now have a usable effect-configuration surface without `fabric-permissions-api`; Bukkit admins gain a permission-less complement to the existing `rtp.effect.*` permission tree.
- **`/rtp info` health block + bStats RTP cost-metric charts** ([`METRICS_PLAN.md`](docs/dev/METRICS_PLAN.md), Phase M1+M2 vertical slice). `/rtp info` now appends four operator-facing live-state lines sourced from a single `RTP.metrics.snapshot()` call: players waiting in region queues (`[queueDepth]`), in-flight teleport pipelines (`[pendingTeleports]`), rolling-mean pipeline latency (`[avgPipelineMs]`, ms), and JVM heap usage (`[heapUsedMb]` / `[heapMaxMb]`). Seven new placeholder tokens — `queueDepth`, `pendingTeleports`, `avgPipelineMs`, `heapUsedMb`, `heapMaxMb`, `memoryEntries`, `chunkLoadBacklog` — are usable in any `messages.yml` line and via PlaceholderAPI. The bStats integration (full id 30865, lite id 12277) registers eleven new charts via `RTPCostMetricsCharts`: configuration adoption (`platform`, `assembly_variant`, `database_backend`), `region_count` (single-line), `cache_pool_health` (multi-line, L1 / L2 % fill), and bucketised cost histograms (`tps_buckets`, `mspt_buckets`, `pipeline_latency_buckets`, `memory_tracker_pressure`, `chunk_load_backlog_pressure`, `queue_depth_pressure`). All bucketing is privacy-safe (no raw scalars, no server / region / world identifiers); chart lambdas read pre-computed snapshot fields and never call platform APIs inline. Per-platform `MetricsBinding` for TPS (Folia, Fabric, Spigot fallback) and the runtime-health charts that depend on it (e.g. `s005_violations_recent`) remain deferred to a follow-up — TPS lambdas use a reflective `Bukkit.getServer().getTPS()` lookup as an interim probe and report `unknown` when unavailable.
- **bStats pipeline_latency_buckets chart implements ADR-032** ([ADR-032](docs/adr/ADR-032-teleport-pipeline-latency-histogram.md), Proposed 2026-05-05). A 256-sample wait-free ring-buffer histogram of teleport-pipeline latency feeds the `pipeline_latency_buckets` AdvancedPie chart described above; the ring is never reset and ages out via overwrite. Privacy-safe (bucketised only).

### Changed

- **`effects-api` platform-split for Fabric readiness** (effects-api-ADR-003; effects-api-ADR-004 amends ADR-002). `Effect` and `EffectFactory` moved to `effectsapi.common` and decoupled from `org.bukkit.*` (now `implements Runnable, Cloneable`); Bukkit-side concretes, listeners, commands, and events relocated under `effectsapi.bukkit.*`; token coercion routed through a per-platform `ValueCoercer` SPI; Bukkit registration and scheduling extracted to `BukkitEffectsInitializer`. **Addon migration:** import prefix changes from `io.github.dailystruggle.effectsapi.{LocalEffects,BukkitListeners,commands,events}.*` to `io.github.dailystruggle.effectsapi.bukkit.*`; subclasses that called inherited `BukkitRunnable` methods (`runTaskTimer`, `cancel`, `isCancelled`, etc.) directly will no longer compile; subclasses that only override `run()` are unaffected.
- **Bundled `docs/` tree inside release jars trimmed to admin- and addon-author-facing material.** `rtp-plugin`'s `copyDocs` no longer ships internal dev docs.
- **`MULTI_SERVER_PLAN.md` updated to treat multi-proxy deployments as first-class** (docs only; D-005 gated, no code changes). Adds a *Multi-Proxy Deployment* section covering HA / failover, geo-distributed proxies, capacity scaling, and blue/green proxy upgrades; introduces a per-proxy `proxyId` field in the `network.yml` straw-man (analogous to per-backend `serverId`); locks in design rules for proxies (no proxy-to-proxy chatter, no singleton assumption, idempotent operations, advisory-only local state, per-proxy heartbeat row); documents hot-spot avoidance under multiple proxies and reservation-token / network-wait-queue behaviour under proxy races and proxy death; adds REQ-RTP-NET-009 / REQ-RTP-NET-010 stubs; bumps the Phase 2 acceptance baseline to `2× Velocity (behind L4) + 2× Paper`.
- **`MULTI_SERVER_PLAN.md` documents DragonflyDB (and other RESP-compatible servers) as supported Redis-transport drop-ins** (docs only; D-005 gated, no code changes). The `redis` transport type covers Redis, DragonflyDB, and KeyDB through the same `RedisNetworkStateBinding` — only the connection URL differs, no `transport.flavour` knob, no per-flavour code branches. Redis remains the reference implementation; Phase 2 acceptance gains a parallel container exercising the cross-server `/rtp` round-trip against DragonflyDB on the unmodified binding, with any observed RESP divergence (Lua/`EVAL`, Streams consumer groups, persistence semantics) recorded in `LESSONS_LEARNED.md` rather than branched on. The post-implementation Postgres-vs-Redis comparative benchmark grows a third row for Dragonfly to give operators evidence-based guidance on when its multi-threaded single-node design is worth choosing.
- **Fabric is supported by the RTP-lite assembly variant** ([ADR-024](docs/adr/ADR-024-rtp-lite-assembly-variant.md), 2026-05-07 amendment). The lite jar now ships `RTPFabricMod`, the `rtp-fabric-common` classes, and `fabric.mod.json`, so Fabric Loader can discover RTP from the unclassified `RTP-<version>.jar` instead of requiring `RTP-Pro`. JDBC drivers (H2 / SQLite / MySQL / PostgreSQL) remain stripped from lite, so Fabric+lite operators land on `FabricDatabaseHandler.setupDatabase`'s flat-file YAML fallback (a loud warning is logged); operators who want a SQL backend on Fabric must run the Pro jar. Folia remains full-only.
- **Claim-plugin integrations are supported by the RTP-lite assembly variant** ([ADR-024](docs/adr/ADR-024-rtp-lite-assembly-variant.md), 2026-05-11 amendment). Lite now invokes `ClaimIntegrations.setup` from `RTPBukkitLitePlugin.onEnable` (deferred tick+1) and declares `softdepend: [ GriefDefender, GriefPrevention, Towny, HuskTowns, Factions, Lands, RedProtect, WorldGuard ]` in its `plugin.yml`; `integrations.yml` is bundled in the lite jar so `ClaimIntegrations#buildParser` can load its defaults from the classpath. Pro stays the superset target for large-scale deployments (proxy networks, Folia, SQL/Redis persistence); claim-protection support is a baseline operator expectation on the small Spigot/Paper servers lite primarily serves, so withholding it pushed those operators to Pro for an integration that has zero runtime cost when no claim plugin is installed. Vault/economy and PlaceholderAPI remain Pro-only.
- **Multilingual support (language options) is supported by the RTP-lite assembly variant** ([ADR-024](docs/adr/ADR-024-rtp-lite-assembly-variant.md), 2026-05-11(b) amendment supersedes the original ADR-020 drop). Lite now ships `lang/**` and `language.yml`, and the full (un-trimmed) `messages.yml` from `rtp-plugin/src/main/resources`. No explicit bootstrap call is required: `LanguageBootstrap.resolve(pluginDirectory)` is invoked unconditionally from `Configs#reloadConfigs()`, and `LanguageCmd` (the `/rtp lang` subcommand) lives in `rtp-core` and is registered via the shared command tree, so it is available on lite identically to Pro. Lite-irrelevant `messages.yml` keys (e.g. `notEnoughMoney`, PAPI status, SQL-persistence messages) are unreachable at runtime because the underlying subsystems are not wired; their presence in the file is harmless and matches the "lite contains the featureset, simply not configured by default" stance. The `liteJarStructureCheck` audit no longer forbids `lang/**` or `language.yml`. Pro stays the superset target for large-scale deployments; locale parity is a baseline operator expectation that previously pushed multilingual servers to Pro for a feature with zero runtime cost on single-locale installs.
- **Fabric obf/unobf carrier split** ([rtp-fabric-ADR-009](rtp-fabric/docs/adr/rtp-fabric-ADR-009-obf-unobf-common-split.md), [effects-api-ADR-006](effects-api/docs/adr/effects-api-ADR-006-fabric-obf-unobf-split.md)). Two new Loom-built carrier modules — `rtp-fabric/rtp-fabric-common-unobf/` and `effects-api/effects-api-fabric-unobf/` — host Mojmap-unobfuscated mirrors of the NM-typed surfaces (`FabricRTPWorld`, `FabricRTPChunk`, `FabricRTPPlayer`, `FabricEffectRuntime`, the four `LocalEffects`, `FabricValueCoercer`, `FabricRegistryCompat`, `FabricLegacyText`, `FabricPaletteNormalizer`, `FabricAnvilColumnProbeAdapter`) so the deobfuscated MC 26.x runtime family links cleanly without intermediary `class_NNNN` aliases. Intermediary-bearing 1.20.x / 1.21.x runtimes continue to use the original obf-carrier modules unchanged; runtime selection is dispatched via the new `FabricVersionAdapter#installEffectsWiring(Object)` SPI (default `false`, overridden only by `V26_1_R1FabricVersionAdapter` to install the unobf path). Non-API: `rtp-fabric-common[-unobf]` and `effectsapi.fabric*` are internal carriers, so addon authors require no migration. Carrier-disjointness is regression-guarded by `EffectsApiFabricCarriersDisjointTest` and `RtpFabricCarriersDisjointTest`.
- **Translations expanded.** New locale trees added since beta.1: Japanese (`ja`), Korean (`ko`), Simplified Chinese (`zh`), and a cat dialect (`cat`); existing locales (`de`, `es`, `fr`, `nl`) gained additional `*.lang.yml` overlays. Per the *Known Issues* note below, localized overlays still cover only the static config files (`config.yml`, `safety.yml`, `performance.yml`, `messages.yml`, `effects.yml`) — not the dynamic `worlds/`, `regions/`, `effects/` directories.

### Fixed

- **`safety.yml` and biome-filter edits now invalidate the persisted shape cache** (closes the `[3.0.0-beta.1]` `Known Issues` entry; was a tracked follow-up in `docs/dev/POTENTIAL_BUGS.md`). `RegionCacheKey.canonicalize(...)` now folds a deterministic snapshot of the validity-affecting subset of `safety.yml` — `safetyRadius`, `platformRadius` / `platformAirHeight` / `platformDepth` / `platformMaterial`, `airBlocks`, `unsafeBlocks`, `anvilPrefilterEnabled`, `biomeWhitelist`, `biomes` — into the shape-cache hash inputs, with list values lower-cased and sorted so re-orderings do not invalidate cached `MemoryShape` data. Cosmetic and runtime-only keys (`invulnerabilityTime`, `staleChunkRetryLimit`, `version`) are explicitly excluded. `RegionCacheKey.SCHEMA_VERSION` is bumped from `1` to `2`, so every existing `<region>_<seed>_<12hex>.bin` / `.scan` file and every `rtp_cached_locations` row from a beta.1 install is naturally orphaned on first launch and re-scanned under the corrected predicate. An allowlist regression guard fails CI if a future `SafetyKeys` constant is left unclassified. Covered by `RegionCacheKeyTest` (10 cases).
- **Effect permission tokens with skipped earlier inputs no longer wreck the rest of the chain** (effects-api-ADR-002). `Effect.setData(String...)` now walks a per-effect `KEY_ORDER` with a non-rewinding cursor, assigning each token to the first remaining key whose default type accepts it. Removes a `printStackTrace` path. Covered by `EffectsApiAdaptiveReadingOrderTest`.
- **Per-permission effects did not fire under RTP-lite.** Lite bootstrap now schedules `BukkitEffectsHandler.setupEffects` at tick+1 and re-introduces `effectParsing: true` in the lite `performance.yml`.
- **Fabric platform bring-up — intermediary mapping parity across hot-path entry points** (rtp-fabric-ADR-009 SPI extension; consolidates work that brought the Fabric adapter from non-functional in beta.1 to a working `/rtp` end-to-end). Several call sites in `rtp-fabric-common` previously dispatched through reflection on mojmap names (`getPlayer`, `getUUID`, `getCommands`, `createCommandSourceStack`) and silently failed on intermediary 1.20.x / 1.21.x runtimes where those methods are obfuscated to `method_xxxxx`. Replaced each with a typed `FabricVersionAdapter` SPI method (`extractPlayerFromConnection`, `getPlayerUUID`, `resolveSenderUuid`, `dispatchConsoleCommand`, `installEffectsWiring`) dispatched via `FabricVersionAdapterRegistry.peek()`; per-version adapters (`v1_20_R1`, `v1_21_R1`, `v1_21_R5`, `v1_21_R11`, `v26_1_R1`) implement each override with direct typed calls so Loom remaps descriptors to the runtime's mapping at compile time. Covers join/disconnect player tracking (`FabricEventBridge`), `FabricServerAccessor.{register,unregister}PlayerObject`, `/rtp` sender resolution in `FabricBrigadierSourceBridge`, and post-teleport `consoleCommands` dispatch. Zero reflection on the relevant hot paths. Regression guarded by `FabricEventBridgeAdapterDispatchTest`.

- **Default permissions on Fabric now mirror `plugin.yml` when no permissions mod is installed.** `FabricRTPPlayer.hasPermission` (and the unobf / 26.1 mirrors) previously fell through to the `ops.json` op-check whenever no `fabric-permissions-api` implementer (LuckPerms-Fabric, Cyan, Ledger, ...) was registered. Baseline player nodes such as `rtp.see` and `rtp.use` (declared `default: true` in `plugin.yml`) were therefore silently denied to non-ops, breaking `/rtp` on a vanilla-permissions Fabric install; conversely, `rtp.onevent.*` (declared `default: false`) would have been granted to ops if anything queried them, contradicting the descriptor. Added a shared `FabricDefaultPermissions` table (mirrored as `FabricDefaultPermissionsUnobf` on the unobf classpath) that resolves each node to `TRUE` / `FALSE` / `OP` straight from `plugin.yml` defaults. Resolution order is now perms-api -> default table -> `ops.json`: `TRUE` grants to everyone, `FALSE` denies even ops, `OP` falls through to the existing `ops.json` scan, and unknown nodes (addon-defined, wildcard-expanded `rtp.worlds.<world>` / `rtp.regions.<region>`, etc.) default to `OP` to match their parent wildcard's convention.

### Known Issues

**Fabric-only:**

- **Permission-based effects on Fabric require a `fabric-permissions-api` implementation.** All nine pipeline-stage hooks (`rtp.effect.presetup` / `postsetup` / `preload` / `postload` / `preteleport` / `postteleport` / `cancel` / `queuepush` / `queuepop`) are wired on Fabric identically to Bukkit (`FabricEffectsHandler.setupEffects`), but `rtp.effect.<stage>.<token>` permission tokens are only consulted when a permissions-api implementer (LuckPerms-Fabric, Cyan, Ledger, ...) is installed. On a vanilla-permissions Fabric server the union falls back to `effects/<group>.yml` (effects-api-ADR-005) only — which is the recommended configuration surface on Fabric regardless.
- **SQL database backends are not available on Fabric.** Only the SQLite fallback is wired. MySQL, PostgreSQL, H2, Redis, and YAML accessors are **unplanned** for Fabric. Bukkit-family platforms are unaffected.
- **Block-tag registry occasionally logs `Block-tag registry yielded no usable tags` during Fabric startup.** Tag resolution races mod / datapack registry population on some load orders; RTP retries resolution after server startup completes, so the warning is informational and tag-driven safety predicates remain in effect once startup finishes. Bukkit-family platforms are unaffected.
- **Performance considerations on Minecraft 1.20.1 (Fabric).** On 1.20.1 specifically, holding chunk tickets at the level used by other platforms causes vanilla's tick pipeline to retain `CompletableFuture` dependent graphs proportional to held tickets, producing significant heap growth over time on otherwise idle servers. RTP's 1.20.1 adapter mitigates this by holding kept-cache tickets at `BORDER` level (loaded but not entity-ticking), which preserves teleport correctness but means kept-cache chunks do not tick mobs, redstone, or weather while reserved. 1.21.x is unaffected. Operators on 1.20.1 should expect somewhat higher baseline memory use than on 1.21.x even after the mitigation, and may want to keep `cacheCap` / `keptCache` modest until they upgrade.

**All platforms:**

- **Translations do not apply to dynamic config directories** (`worlds/`, `regions/`, `effects/`). Localized `lang/<locale>/*.lang.yml` overlays cover the static config files (e.g. `config.yml`, `safety.yml`, `performance.yml`) but per-world, per-region, and per-effect files generated under those directories remain in the default language regardless of the configured locale. Affects Bukkit-family platforms (Spigot, Paper, Folia) and Fabric.

---

## [3.0.0-beta.1] - 2026-05-03

> ?? **Major release - breaking `rtp-api` changes.** See [MIGRATION.md](docs/admin/MIGRATION.md#upgrading-to-300-beta) for upgrade instructions.

### Beta Scope

This is a **beta** release. The following surface is considered **in scope** and supported:

- **Platforms:** Spigot, Paper, Folia (Minecraft 1.20.x / 1.21.x / 26.1, Java 21+).
- **Persistence:** H2 (default) and SQLite. MySQL, PostgreSQL, Redis, and YAML fallback ship but are **experimental** - production use not recommended.
- **Public API:** the `rtp-api` types listed under [Added](#added). Internals (`rtp-core`, platform adapters) may still change between betas.
- **Claim integrations** (folded into the plugin per ADR-019): Factions, GriefDefender, GriefPrevention, HuskTowns, Lands, RedProtect, TownyAdvanced, WorldGuard.
- **Anvil pre-filter** on Spigot (ADR-016).
- **Brigadier bridge** for Paper/Folia (commands-api-ADR-001).

**Out of scope for this beta** (do not file as bugs):

- **Fabric (`rtp-fabric`)** - present in the source tree but **not functional**. `/rtp` does not yet teleport on Fabric (scheduled-task processors not wired; permissions, teleport-cancel callback, and full Brigadier command tree pending). Tracking: [`docs/dev/MULTI_PLATFORM_PLAN.md`](docs/dev/MULTI_PLATFORM_PLAN.md). Do not deploy on Fabric.
- **Native Forge / NeoForge adapter** - not planned, and **lower priority than multi-server / proxy support**. Forge-based servers are supported via Bukkit-compatibility layers such as **Arclight** or **Mohist** (run RTP as the Spigot/Paper build on those launchers). A dedicated Forge/NeoForge adapter remains deferred (ADR-022, MULTI_PLATFORM_PLAN Phase 4) and may not materialize if compatibility layers continue to cover the use case.
- **Multi-server / proxy mode** (Velocity, BungeeCord cross-server queues, reservation tokens) - design only. See [`docs/dev/MULTI_SERVER_PLAN.md`](docs/dev/MULTI_SERVER_PLAN.md).
- **Legacy Minecraft (< 1.20) and legacy Java (< 21)** - see [ADR-021](docs/adr/ADR-021-legacy-mc-and-java-support-scope.md).
- **`RTPEconomy`** API - present but marked *experimental*; signature may change before `3.0.0` final.
- **`/rtp config` command** - present but **experimental**; subcommand surface, argument grammar, and write-back semantics may change before `3.0.0` final. Prefer editing config files directly for production use.
- **Lite assembly variant** ([ADR-024](docs/adr/ADR-024-rtp-lite-assembly-variant.md)) - produced from this build but not yet published on a fixed cadence. From `3.0.0` final onward, the Lite jar will be published as **`RTP`** (the **free tier**) a few hours-to-days after the corresponding **`RTP-Pro`** (full / paid tier) jar of the same version.

### Known Issues

User-visible issues that exist in this beta and are **not** considered release-blockers. File a new bug only if your symptom differs from the descriptions below.

- **Fabric adapter is non-functional.** See *Out of scope* above. Bukkit-family platforms are unaffected.
- **MySQL / PostgreSQL / Redis / YAML accessors are experimental.** Schema and migration semantics may change before `3.0.0` final. Stick with H2 or SQLite for production.
- **`safety.yml` and biome-filter edits do not invalidate the persisted shape cache** ([`POTENTIAL_BUGS.md`](docs/dev/POTENTIAL_BUGS.md), 2026-04-30 entry). Geometry and vertical-adjustor edits are caught (ADR-022); changes to `unsafeBlocks`, `platform`, `requireSkyLight`, ADR-017 tag/state predicates, or per-region biome whitelists/blacklists may leave stale "bad sector" flags in `<region>_<seed>_<hash>.bin`. **Workaround:** `/rtp scan reset <region>` after changing safety predicates.
- **Tag-grammar tokens in `safety.yml`** (e.g. `#minecraft:flowers`) are accepted but inert until the config-reader tag-expansion slice lands (`SAFETY_TAGS_AND_STATES_PLAN.md` Slice 3). Use explicit material names for now.
- **Two ADR files share the number `ADR-022`** ([`POTENTIAL_BUGS.md`](docs/dev/POTENTIAL_BUGS.md), 2026-05-01). Documentation only; no runtime effect. Cross-references in this changelog disambiguate by filename.
- **Pre-release cache invalidation:** legacy 2.x `.bin` files and existing `rtp_cached_locations` rows from any pre-`3.0.0-beta.1` build are abandoned by ADR-022's hash-keyed naming scheme. Expect a one-time re-scan on first launch.
- **Emergency landing platform default changed.** `safety.yml` now ships `platformRadius: -1` (disabled). Set `platformRadius: 0` or higher to restore legacy 2.x behaviour.

For a live backlog of incidental bugs spotted during development (not necessarily user-visible), see [`docs/dev/POTENTIAL_BUGS.md`](docs/dev/POTENTIAL_BUGS.md).

### Planned for `3.0.0` final and beyond

Roadmap visibility for users and addon authors. Order is intent, not commitment.

- **Fabric platform** to first-class (`rtp-fabric`) - **targeted for `3.0.0` final**. Active frontier - see [`MULTI_PLATFORM_PLAN.md`](docs/dev/MULTI_PLATFORM_PLAN.md). Remaining: scheduled-task processor parity (Step E3), permissions via `fabric-permissions-api` (Step F), full Brigadier command tree (Step G2), stabilization smoke test (Step H).
- **Fold `safety.yml` validity fields and biome filters into the region cache hash** ([`POTENTIAL_BUGS.md`](docs/dev/POTENTIAL_BUGS.md) follow-up to ADR-022).
- **Multi-server / proxy mode** (Velocity, BungeeCord) - phased rollout per [`MULTI_SERVER_PLAN.md`](docs/dev/MULTI_SERVER_PLAN.md). D-005 gated; admin docs stub at [`docs/admin/proxies/INDEX.md`](docs/admin/proxies/INDEX.md).
- **Runtime metrics** (TPS / MSPT / heap / queue depth / pipeline samples) - [`METRICS_PLAN.md`](docs/dev/METRICS_PLAN.md).
- **External hook API surface** ([ADR-026](docs/adr/ADR-026-external-hook-api-surface.md), [`EXTERNAL_HOOKS.md`](docs/dev/EXTERNAL_HOOKS.md)) - formal registry for third-party reflection / soft-depend integrations.
- **Lite assembly variant** ([ADR-024](docs/adr/ADR-024-rtp-lite-assembly-variant.md)) - trimmed jar for resource-constrained servers, **published as `RTP` (the free tier)** a few hours-to-days after the corresponding **`RTP-Pro` (full / paid tier)** jar of the same version. The Pro jar ships first; the free Lite jar (`RTP`) follows once the trimmed assembly is verified against the same release tag.
- **Promote MySQL / PostgreSQL / Redis / YAML** accessors out of *experimental*.
- **Stabilize `RTPEconomy`** API.
- **Stabilize `/rtp config` command** - finalize subcommand surface, argument grammar, and write-back semantics, then drop the *experimental* label.
- **Forge / NeoForge** - **lower priority than multi-server / proxy support.** No native adapter planned for the foreseeable future; Forge-based servers should use a Bukkit-compatibility launcher (Arclight, Mohist, or equivalent) and run the Spigot/Paper build. A dedicated adapter is only re-evaluated if those compatibility layers stop covering the platform ([rtp-fabric-ADR-002](rtp-fabric/docs/adr/rtp-fabric-ADR-002-platform-in-scope.md)).

### Highlights

1. **Versioned platform submodules** - compile-time NMS separation via a `common` module plus per-version submodules for Spigot, Paper, and Folia (ADR-010).
2. **Stable public `rtp-api`** - broadened addon surface; no more reflection into internals (ADR-011).
3. **Multi-backend persistence** - flat-file sector cache replaced by SQL-backed storage (H2 default, SQLite, MySQL, PostgreSQL) with Redis caching and YAML fallback (ADR-002). *H2 and SQLite are production-ready; MySQL/PostgreSQL/Redis/YAML remain experimental.*
4. **Per-platform scheduling pipelines** - Folia uses regional-thread scheduling; Paper uses native async chunk loading (PaperLib removed); Spigot uses the Bukkit scheduler (ADR-004 - 006).
5. **Lock-free location buffer & state-based tasks** - `LockFreeLocationBuffer` decouples pre-generation from teleport dispatch; explicit task states enable pause/resume/cancel and concurrent region fills.
6. **`ChunkReservation` lifecycle** - `AutoCloseable` abstraction eliminates the 2.x ticket leak (ADR-012).
7. **Multi-platform expansion** - `rtp-fabric` adapter (in progress) and `commands-api` / `effects-api` consolidated as sub-modules.
8. **Anvil read-only pre-filter** - off-tick safety probing of unloaded chunks on pure Spigot (ADR-016).

Rationale for each decision lives in [`docs/adr/`](docs/adr/README.md).

### Added

**Public API (`rtp-api`)** - new types include `RTPAPI`, `ILocationGenerator`, `GenerationContext` / `GenerationResult`, `RTPScheduler` / `TrackedRTPTask`, `RTPServerAccessor`, `RTPEconomy` *(experimental)*, `RTPCommandSender` / `RTPPlayer`, `RTPWorld` / `RTPChunk` / `ChunkSet`, `RTPCoords` (record) / `MutableRTPCoords`, `RTPLocation`, `MessagesKeys`, `RTPRunnable`, `PerformanceTracker`, and `ChunkReservation`. Precondition / postcondition / thread-safety Javadoc added to `RTPAPI`, `ILocationGenerator`, and `ChunkReservation`.

**Platform adapters** - restructured into versioned submodules with a shared `common` module (ADR-010):
- `rtp-folia`, `rtp-paper`, `rtp-bukkit` each ship `-common`, `-v1_20_R1`, `-v1_21_R1`, `-v26_1_R1`.
- New `rtp-fabric` adapter (work in progress; see `docs/dev/MULTI_PLATFORM_PLAN.md`).

**Persistence** - `AbstractSQLDatabaseAccessor` foundation with concrete `H2`, `SQLite`, `MySQL`, `PostgreSQL` accessors, plus `RedisManager` and `YamlFileDatabase` fallback.

**Performance & scheduling** - `LockFreeLocationBuffer`, state-based scan/teleport tasks, per-region Folia rate limiting, per-platform pipelines, and `PerformanceTracker` instrumentation. Region-cache async callbacks and chunk-load wall-clock time are now attributed to `pluginMSPT`. `RTPCoords` is a record to reduce hot-path allocation.

**Commands** - `/rtp scan reset [region]` clears a region's `MemoryShape` bad-sector data without restarting a scan. New operator diagnostic `/rtp test chunk-probe-perf [samples=<n>]` A/B-times `RTPWorld.probeChunkColumn` against `RTPWorld.getChunkAtAsync` over a random sample of pregenerated chunks discovered by scanning the world's `region/` folder; reports pool size, totals, per-sample averages, the full/probe ratio, and the probe-null rate (BIOME_LOOKUP_PERF_PLAN phase 0). Now also times full-anvil `AnvilReader.readChunk` for pairwise comparison.

**Brigadier bridge in `commands-api` (commands-api-ADR-001, REQ-API-ARCH-005).** `BrigadierCommandAdapter` and `BrigadierBridgeContext<S>` (under `commands-api/.../brigadier/`) walk a `commands-api` `TreeCommand` and emit a Brigadier `LiteralArgumentBuilder` with literal sub-command nodes, typed argument nodes for `IntegerParameter` / `FloatParameter` / `BooleanParameter`, string-with-suggestions for `EnumParameter` / `CoordinateParameter` / unknowns, and `requires(...)`-gated permissions. Brigadier is `compileOnly` on `commands-api`, so Bukkit-family runtimes never load the adapter. New `RTPCmdFabric.register(...)` shim in `rtp-fabric-common` exposes a one-call hookup for the eventual Fabric `CommandRegistrationCallback`. Purely additive - `BukkitTreeCommand` remains the production dispatch path on Bukkit-family platforms. Covered by `ReqApiArch005BrigadierBridgeTest` (3 tests). The Fabric `CommandRegistrationCallback` wiring itself stays deferred to Phase 3 / 4 per `MULTI_PLATFORM_PLAN.md`.

**Documentation & governance**
- `docs/dev/REQUIREMENTS.md` with full REQ-ID coverage, including new `REQ-RTP-S-001..S-006` prohibitions cross-linked to `docs/admin/HAZARDS.md`.
- `docs/dev/TRACEABILITY.md` (69 REQ-IDs; addon requirements pending audit), `docs/dev/GLOSSARY.md`, `docs/dev/STAKEHOLDERS.md`, `docs/dev/CONCEPTS.md`, `docs/dev/DESIGN.md`, `docs/dev/COVERAGE_PLAN.md`, `docs/dev/MULTI_PLATFORM_PLAN.md`.
- `docs/adr/` - ADR-001 through ADR-013 and index (`README.md`); commands-api-ADR-001 (Brigadier bridge), ADR-015 (stale-chunk guard), ADR-016 (Anvil pre-filter) added later in the beta cycle.
- `docs/admin/` - `MIGRATION.md`, `QUICK_START.md`, `COMMANDS.md`, `CONFIGURATION.md`, `FAQ.md`, `HAZARDS.md` (H-001..H-010), `FAILURE_MODES.md` (FM-001..FM-009), `RUNBOOK.md` (7 incident scenarios).
- Audience landing pages: `docs/FOR_ADDON_DEVELOPERS.md`, `docs/FOR_CONTRIBUTORS.md`, `docs/FOR_SERVER_ADMINS.md`.
- Root-level `CONTRIBUTING.md`, `SECURITY.md`, `CHANGELOG.md`, and `addons/REQUIREMENTS.md`.
- Module dependency diagram and ADR index link in `docs/dev/ARCHITECTURE.md`.

**CI / tooling** - Gradle workflow (`gradle.yml`) replaces Maven (`maven.yml`) on Java 21 with Spotless, JUnit publishing, and Jacoco upload; `check_traceability.sh` gate in Jenkins and GitHub Actions; `.github/dependabot.yml`, `.github/PULL_REQUEST_TEMPLATE.md`, and `.github/ISSUE_TEMPLATE/config.yml`.

### Changed

- **Claim-plugin integrations folded into `rtp-plugin`** (ADR-019). The `RTP_ClaimPluginIntegrations` addon is removed; the eight checkers (Factions, GriefDefender, GriefPrevention, HuskTowns, Lands, RedProtect, TownyAdvanced, WorldGuard) now register at plugin startup in `ClaimIntegrations`. `integrations.yml` ships with the plugin jar; behaviour for REQ-RTP-S-003 is unchanged (verifiers still live behind `GlobalRegionVerifiers`). A new `addons/RTP_ExampleAddon` replaces the claim addon as the canonical "how to write an addon" template.
- `/rtp fill` ? `/rtp scan` (permission `rtp.fill` ? `rtp.scan`) for clarity.
- `CachedLocation` is now an immutable Java record (breaking for addons that mutated fields directly).
- `rtp-paper`: PaperLib dropped in favour of native Paper async chunk loading.
- Platform version targets upgraded to 26.1 (Spigot, Paper, Folia); Java target raised to **Java 21**.
- Active task tracking moved to `MemoryTracker` with `WeakReference` deallocation; write-through caching consolidated.
- Independent region queues now fill concurrently without a shared global-queue lock.
- `sendMessage` call-site tracking now uses stack trace source for diagnostics.
- Allocation reduced on hot paths (records, primitives, vectors removed); `MemoryShape` pending-write storage switched from maps to sets.
- `Region` decomposed for cohesion.
- **ScanTask diagnostics (BIOME_LOOKUP_PERF_PLAN.md PR-16).** Extended the `[DEBUG_LOG] ScanTask concurrency ...` line with `avgColdMissMs` (average `Files.readAllBytes` wall time per cache miss) and `gcDeltaMs` (cumulative GC time since previous log window) so operators can discriminate between OS page-cache pressure, GC churn, and driver-loop bottlenecks when scan throughput degrades with scan progress. Diagnostic-only, no behavior change.
- **ScanTask per-bin drain removed + stage-2 instrumentation (BIOME_LOOKUP_PERF_PLAN.md PR-17).** PR-16 runtime data ruled out I/O and GC as causes of cps degradation (both metrics stayed flat while `peakInFlight` dropped 50 ? ~25 over three minutes), pointing at driver-side pipeline bubbles. Removed the `inFlightGate.acquire(MAX_PENDING_CHUNKS)` drain between region-file bins in `ScanTask.run`: PR-15 coalescing already makes cross-bin cache interference cheap, so the barrier was costing saturation without earning correctness. Added `fullLoads=N fullLoadAvgMs=X.XX` to the gauge log (AtomicLong counters wrapped around `runFullLoadPath`'s result future) so operators can confirm whether stage-2 cost (loaded-chunk safety scan) grows with scan progress; if `fullLoadAvgMs` climbs while `avgColdMissMs` stays flat, the next optimization target is the full-load path, not the probe.
- **Biome-lookup performance plan (BIOME_LOOKUP_PERF_PLAN.md PR-1 through PR-12).** A sequence of commits retired `Region.maxBiomeChecksPerGen` + `PregenState.maxBiomeChecks` in favor of a single `maxAttempts` knob; added probe-first fast paths in `PregenTask` and `ScanTask` backed by `RTPWorld.probeChunkColumn` (overridden on Bukkit / Paper / Folia to read the chunk's center column directly from the persisted `r.X.Z.mca` via `AnvilReader.readColumnProbe`); wrapped the result through `AnvilColumnProbeAdapter` (applies `PaletteNormalizer::reconcile`); gated on `SafetyKeys.anvilPrefilterEnabled` + `isChunkLoaded`. New `LocationGenerator.FailTypes.prefilterBiome / prefilterBlock / prefilterRange` buckets attribute the rejections. `VerticalAdjustor` gained `requiresSkyLight()`. ScanTask reframes the probe as a load-replacement: on cache-miss the probe is authoritative (skip `getOrLoadChunk`); on cache-hit the safety radius upgrades to `max(configured, 2)` for free. Blocking I/O moved to a dedicated `AnvilIoPool` (`max(8, 2 * CPU)` daemon threads) to avoid `ForkJoinPool.commonPool` starvation. Added `AnvilRegionByteCache` (LRU of raw `.mca` bytes, mtime-invalidated) to eliminate redundant per-chunk `Files.readAllBytes` of the same ~4 MB region file; PR-11 added hit/miss/stale diagnostics surfaced in `ScanTask`'s existing `[DEBUG_LOG] ScanTask concurrency ...` line; PR-12 raised cache capacity from 4 to 16 (~64 MB steady-state) after PR-11 measurement showed the 4-entry LRU thrashing at ~0.31 hit rate because the scan frontier spans 6-10 distinct region files simultaneously. Measured `full/probe` ratio is 16.46x on Bukkit and 20.70x on Folia via `/rtp test chunk-probe-perf`.

- **Region shape cache key now folds shape and vertical-adjustor config (ADR-022).** Persisted `MemoryShape` data and `ScanTask` progress files were keyed only by `<regionName>_<worldSeed>`, so edits to `centerX/centerZ`, `minRadius/maxRadius`, `spatialResolution`, the shape class, or the vertical adjustor silently reused stale per-cell "bad" flags whose spiral mapping no longer matched the configured region. Filenames are now `<regionName>_<seed>_<12hex>.bin` / `.scan`, where the 12-hex suffix is a stable `SHA-256` of `(SCHEMA_VERSION, seed, shape FQCN + sorted shape data, vert FQCN + sorted vert data)`. The same hash, truncated to 64 bits, is written into the existing `rtp_cached_locations.seed BIGINT` column, so cached `RTPLocation` rows are invalidated under the same predicate without a database schema migration. `Region.setSettings(...)` deletes orphaned `.bin` / `.scan` files when the hash changes; `ScanTask.delete(regionName)` now sweeps every `<regionName>_*.scan` file. Region cache hydration moved off the calling thread onto `RTP.scheduler.runTaskAsynchronously` so DB I/O can no longer stall plugin init. Pre-release: legacy `.bin` files and existing rows are abandoned (no migration). `safety.yml` validity fields and biome filters are **not** yet folded into the hash; tracked as a follow-up entry in `docs/dev/POTENTIAL_BUGS.md`.

### Fixed

- **ScanTask probe-first fast path rejected passable non-air blocks (BIOME_LOOKUP_PERF_PLAN.md follow-up).** After the `isAirAt` reconciliation fix unblocked the anvil fast path, live scans still showed a residual `adjustNull` tail (~1300-1600 per gauge tick) whenever the center-column body/head cells held configured-passable blocks like tall grass, flowers, or snow layer. `JumpAdjustor.acceptProbeY` required strict vanilla-air (`isAirAt`) for Y and Y+1, so any chunk whose candidate Y had grass above stone was routed back through the full-load path. `safety.yml`'s `airBlocks` list documents exactly those passable materials but was wired only into the config enum - no code consumed it. Fix: `JumpAdjustor` now caches `SafetyKeys.airBlocks` next to `unsafeBlocks` (shared 5-second refresh in `refreshSafetySets`) and `acceptProbeY` accepts body/head as passable when vanilla air OR present in `airBlocks`, with `unsafeBlocks` taking precedence on conflicts. Tag-grammar tokens like `#minecraft:flowers` are stored verbatim and remain inert until the config reader's tag expansion slice lands (tracked in `docs/dev/SAFETY_TAGS_AND_STATES_PLAN.md` Slice 3). Regression guarded by `JumpAdjustorProbeTest.airBlocksFromConfig_acceptsTallGrassHeadSpace`.
- **ScanTask probe-first fast path silently vetoed by case-sensitive `isAirAt` (BIOME_LOOKUP_PERF_PLAN.md follow-up).** `AnvilColumnProbeAdapter.blockAt` reconciles every palette identifier through `PaletteNormalizer::reconcile`, which converts `"minecraft:air"` to `Material.AIR.name()` = `"AIR"` so that downstream `unsafeBlocks.contains(...)` checks (populated as `Material#name()` upper-case tokens like `LAVA`) match symmetrically. The default `ChunkColumnProbe.isAirAt` then compared the path segment against lowercase `"air"` / `"cave_air"` / `"void_air"` and returned false for every air block, making `JumpAdjustor.acceptProbeY` and `LinearAdjustor.acceptY` find no acceptable Y on any center column. Live runs showed `adjustNull == activeChecks` every gauge tick with `fullLoads == activeChecks`, negating the anvil pre-filter entirely and leaving scan throughput at the PR-17 baseline. Fix: override `isAirAt` in `AnvilColumnProbeAdapter` to recognize the reconciled `AIR` / `CAVE_AIR` / `VOID_AIR` forms in addition to the raw `minecraft:*` path segments. Regression guarded by `AnvilColumnProbeAdapterIsAirAtTest`.
- **Emergency landing platform spam** on kept-queue / DB-rehydrated teleports (Folia in particular). `TeleportPipelineTask.runTeleport` previously gated `RTPWorld.platform(...)` on `reservation == null`, which was always true for those paths. Gate is now a read-only safety check against the already-loaded landing chunk (`RTPWorld.getCachedChunk(...)` + `SafetyKeys.unsafeBlocks`); no chunk loads are triggered (REQ-RTP-S-005 preserved). `Region.execute` synthesizes a `ChunkReservation` for kept-queue pairs so REQ-RTP-S-002 stays intact. Default `safety.yml` now ships `platformRadius: -1` (disabled); set `platformRadius: 0` or higher to restore legacy behaviour.
- Chunk leak in region queue replenishment and in early `ChunkReservation` integration.
- Memory leak in active task tracking.
- Immediate-teleport timing regression and duplicate task execution on queue drain.
- `MemoryShape` caching inconsistencies and region-boundary locational edge cases.
- Erroneous 2D coordinate check that rejected valid surface locations.
- **On-event teleports granted to operators by default (Paper + LuckPerms).** `ParsePermissions.hasPerm` called `sender.hasPermission(...)` directly for each candidate child node. On Paper with LuckPerms (and any permission manager that defers to op state for unset nodes), `hasPermission` short-circuits to `true` for ops regardless of the registered `default: false` we declared in `plugin.yml`. The result was that every registered on-event handler (`firstjoin`, `join`, `respawn`, `changeworld`, `move`, `teleport`) auto-fired for any op the moment they triggered the event, even when the admin had explicitly not granted the corresponding `rtp.onevent.*` node. Fix: rewrote `ParsePermissions.hasPerm` to iterate the sender's *effective* permissions (`sender.getEffectivePermissions()`, which only contains explicitly granted nodes) and match each candidate child against that set - the same pattern already used by `ParsePermissions.getInt` for numeric permission tiers. This bypasses the permission manager's op-default short-circuit entirely, restoring the documented opt-in `default: false` behaviour. The earlier removal of the `permissionPrefix + "*"` wildcard fast-path is retained on general principles but was not load-bearing on its own.
- **`Lifetime Chunks Loaded` (`/rtp info`) double-counted every live chunk load.** `RTPWorld.getOrLoadChunk` (added in the ADR-016 §13.1 follow-up) calls `getChunkAt` (probe entry) and, on UNKNOWN, `getChunkAtAsync` (live entry) for the same logical attempt. Every adapter incremented `RTPWorld.totalChunkLoads` from `getChunkAt`, and on Spigot/Fabric `getChunkAtAsync` delegated back through `getChunkAt`, so each cache-miss load was counted twice (~2.09× ratio observed in the field: `cached: 110`, `Lifetime Chunks Loaded: 230`). The counter is now incremented exactly once per live load, in the live-load path itself: `BukkitRTPWorld#loadChunkFuture`, `FoliaRTPWorld#loadLiveChunk` and `FoliaRTPWorld#getChunkAtAsync`, and inside the `MinecraftServer#submit` body of `FabricRTPWorld#getChunkAt`. Probe-only paths (anvil cache hits) and kept-cache replays no longer contribute. Operators upgrading will see `infoTotalLoads` drop to roughly half its previous value - this is the corrected reading, not a regression. The `leakRate` placeholder is unaffected (still divided by `lifetimeTicketsIssued`). Javadoc on `RTPWorld.totalChunkLoads` updated to document the new "actual live loads only" semantics.
- Initial command execution failure on first plugin load.

---

## [2.0.18] - 2024-01-01

> Last 2.x release. All changes since this tag are captured under `[3.0.0-beta.1]` above.
>
> **Upgrading from 2.0.18?** See [MIGRATION.md](docs/admin/MIGRATION.md#upgrading-to-300-beta).

---

## [2.0.x] - Historical

Earlier versions introduced the multi-module split (`rtp-api` / `rtp-core` / platform adapters), the asynchronous queue system, the Archimedean spiral shape algorithm, and the `MemoryShape` persistent cache. Per-commit history is available via `git log`.

---

[3.0.0-beta.4]: https://github.com/DailyStruggle/RTP/compare/v3.0.0-beta.3...HEAD
[3.0.0-beta.3]: https://github.com/DailyStruggle/RTP/compare/v3.0.0-beta.2...v3.0.0-beta.3
[3.0.0-beta.2]: https://github.com/DailyStruggle/RTP/compare/v3.0.0-beta.1...v3.0.0-beta.2
[3.0.0-beta.1]: https://github.com/DailyStruggle/RTP/compare/v2.0.18...v3.0.0-beta.1
[2.0.18]: https://github.com/DailyStruggle/RTP/releases/tag/v2.0.18
