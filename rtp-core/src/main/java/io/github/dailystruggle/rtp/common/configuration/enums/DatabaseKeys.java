package io.github.dailystruggle.rtp.common.configuration.enums;

/** Configuration keys for database/persistence settings (database.yml). */
public enum DatabaseKeys {
  /** Database backend settings map (type/host/port/name/username/password). */
  database,
  /** The configuration version */
  version
}
