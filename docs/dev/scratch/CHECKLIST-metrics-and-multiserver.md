# Implementation Checklist — Metrics + Multi-Server (state-preservation working note)

> **Effective Issue (1-line)**: implement RTP runtime metrics SPI and proxy/multi-server support, in that order, against the design captured in `docs/dev/METRICS_PLAN.md` and `docs/dev/MULTI_SERVER_PLAN.md`.
>
> **Mode when executing**: `[CODE]` for build steps, `[ADVANCED_CHAT]` or `[CODE]` for design/doc steps.
>
> **Blocking decisions awaiting user approval (Rule D-005)**:
> - ADR-025 (multi-server proxy support) must be drafted and approved **before** any code under M-NET-Phase-0 ships.
> - Any `rtp-api` / `rtp-core` SPI shape change inside the metrics work needs explicit D-005 approval at its M-METRICS-Phase-M0 review item.
>
> **Scope rules**: per `AGENTS.md > Stay-On-Task Policy`, incidental bugs go to `POTENTIAL_BUGS.md`, not inline fixes. Per `AGENTS.md > SESSION ARTIFACTS & CLEANUP`, **delete this file once the entire checklist is submitted** (or after the proxy beta release, whichever comes first).
>
> **How to resume after disconnect**: read this file top-to-bottom, re-verify the last `[x]` item still holds (file exists, build green, test still passes), then continue from the first `[ ]`. Tick boxes only after the verification command in the row succeeds.

---

## Conventions

- Each row carries a verification artifact (test name, file path, command, or external URL) so a fresh agent can confirm it without re-deriving context.
- Box state legend: `[ ]` not started, `[~]` in progress, `[x]` verified done, `[!]` blocked or paused (always paired with a Notes line).
- Numbered hierarchy is stable — never renumber. Insert sub-items as `1.1.1`, etc.
- Sequencing rule: items in different sections may run in parallel where dependencies allow; within a section, run top-to-bottom unless a dependency is explicit.

---

## Section A — Metrics Plan, Phase M0 (SPI shape, docs only)

Source-of-truth: [`docs/dev/METRICS_PLAN.md`](../METRICS_PLAN.md).

- [x] **A1.** Confirm the M0 metric catalogue is a *strict superset* of `rtp test full`'s current console output. Verification: open `rtp test full` invocation site (`grep` for `rtp test full` in `rtp-core/src/main/java/...`), diff fields against `MetricsSnapshot` proposal in `METRICS_PLAN.md > Metric Catalogue (v1)`. Record any missing field by amending the plan **before** writing code. *(verified 2026-05-15: `TestFullCmd` emits only per-subcommand audit pass/fail lines via `FullAudit`; no TPS / MSPT / heap / queue / pipeline numeric fields. v1 catalogue is trivially a strict superset; plan's M0 box ticked at `METRICS_PLAN.md` line 403.)*
- [x] **A2.** Decide `avgPipelineMs` window length and reset semantics (open item in `METRICS_PLAN.md > Open Items / Follow-Ups`). Verification: amend the plan with the resolved value; reference this checklist row in the plan diff message. *(resolved 2026-05-15: ADR-032 — 256-sample wait-free ring, never resets, mean-only readout. `METRICS_PLAN.md > Open Items` row updated to *resolved (2026-05-15)*.)*
- [x] **A3.** Decide `databaseLatencyMs` cadence (every-write vs. dedicated probe). Verification: same as A2. *(resolved 2026-05-15: Option 1 — every-write, routed through a single function on `AbstractSQLDatabaseAccessor` so a dedicated probe can be substituted later without touching call sites. Limitation under pool saturation surfaced in field Javadoc + `/rtp info` colour-band guidance. `METRICS_PLAN.md > Open Items` updated.)*
- [x] **A4.** Decide Folia per-region detail opt-in default (`metrics.folia.includeRegions`). Verification: same as A2. *(resolved 2026-05-15: default `true` — Folia operators are the audience that most wants per-region detail; cost bounded by region count. `METRICS_PLAN.md > Open Items` updated.)*
- [x] **A5.** D-005 review of the proposed `MetricsSnapshot` shape (no platform imports; record-style; addons can depend on it). Verification: brief written approval from the user; record decision date in the plan's *Resolved* section. *(approved 2026-05-15: the 16-field on-disk record (`tps{1,5,15}m`, `mspt`, `tickBudgetUtilisation`, `playerCount`, `softCap`, `heapUsed/MaxBytes`, `queueDepth`, `pendingTeleports`, `memoryTrackerEntries`, `chunkLoadBacklog`, `avgPipelineMs`, `databaseLatencyMs`, `takenAtEpochMs`) is approved as-is. Later phases extend via additive constructor changes contained to the single binding constructor. Recorded at `METRICS_PLAN.md` Sufficiency Audit line 667.)*

> **Gate**: Section A complete before any Section B code lands.

---

## Section B — Metrics Plan, Phase M1 (core + Paper; Spigot fallback nice-to-have)

> **Scope amendment (2026-05-16, late session)**: Spigot has been **deprioritized** by the user. Paper is the required platform for Phase M1 acceptance; the raw-Spigot `BukkitTpsSampler` fallback branch remains in the codebase and will be tested by the user at final-results time, but **a Spigot devstack transcript is no longer a B → C gate requirement**. If the Spigot path breaks during final verification it is a nice-to-have fix, not a release blocker.

- [x] **B1.** Create `rtp-core/src/main/java/.../metrics/` package skeleton: `Metrics`, `MetricsSnapshot` (immutable record), `PipelineHistogram`, `HeapSampler`. Verification: `:rtp-core:compileJava` green; ArchUnit guard run shows no platform imports inside the new package. *(verified 2026-05-16: `rtp-core/src/main/java/io/github/dailystruggle/rtp/common/metrics/` contains `Metrics.java`, `MetricsSnapshot.java`, `MetricsBinding.java`, `PipelineHistogram.java`, `HeapSampler.java`, `CoreMetrics.java`; no `org.bukkit.*` imports observed. Landed 2026-05-01 per `METRICS_PLAN.md > Sufficiency Audit` line 402. ArchUnit guard re-run deferred to next session that touches the package.)*
- [x] **B2.** Implement `PipelineHistogram` (256-sample ring buffer, never resets — straw-man, confirmed in A2). Verification: new unit test under `rtp-core/src/test/.../metrics/PipelineHistogramTest.java` covers fill/wrap/percentile semantics. *(verified 2026-05-16: `PipelineHistogramTest` exercises fill, wrap past `CAPACITY`, and `totalRecorded()` vs `sampleCount()` divergence — semantics match ADR-032 (never-resets, mean-only). Ratified by [ADR-032](../../adr/ADR-032-teleport-pipeline-latency-histogram.md).)*
- [x] **B3.** Wire `PipelineHistogram` into `TeleportPipelineTask` completion path. **Additive only** — no existing assertion changes. Verification: `:rtp-core:test --tests "*TeleportPipelineTask*"` green; histogram count increments observable via a new test that runs one synthetic pipeline. *(verified 2026-05-16: `TeleportPipelineTask.runCleanup` lines 624–627 record into `RTP.metrics.pipelineHistogram()` once per task via `pipelineHistogramRecorded` AtomicBoolean. Covered by `TeleportPipelineTaskPhaseTest#runCleanup_records_one_sample_into_pipeline_histogram` and `#runCleanup_is_idempotent_for_pipeline_histogram`. Ratified by ADR-032.)*
- [x] **B4.** Implement `HeapSampler` via `ManagementFactory.getMemoryMXBean()`. Verification: returns sane values in unit test; no platform imports. *(verified 2026-05-16: `HeapSampler` + `HeapSamplerTest` present in `rtp-core/.../metrics/`; static helpers `heapUsedBytes()`/`heapMaxBytes()` consumed by `MetricsSnapshot` and `Metrics.NOOP`.)*
- [x] **B5.** Implement `Metrics.snapshot()` aggregating B1–B4 plus delegated platform binding via `RTP.serverAccessor.getMetricsBinding()`. Verification: snapshot is immutable (record); calling twice with no state change returns equal snapshots. *(verified 2026-05-16: `CoreMetrics.snapshot()` aggregates `pipelineHistogram.mean()` + `HeapSampler` + per-region `queueManager.playerQueue.size()` + `MemoryTracker.trackedCount` + binding fields into a single `MetricsSnapshot`. `MetricsSnapshot` is `final` with `final` fields. Covered by `CoreMetricsTest` and `MetricsSnapshotTest`. **Deviation logged in B6.**)*
- [x] **B6.** *(rescoped 2026-05-16, D-005 approved in-session)* Provide a platform-agnostic hook for adapters to install their `MetricsBinding`. Implementation chose the **push** pattern: `CoreMetrics.setBinding(MetricsBinding)` (`rtp-core/.../metrics/CoreMetrics.java` lines 33–35), invoked by the platform bootstrap at startup, rather than a pull-style `AbstractServerAccessor.getMetricsBinding()`. Rationale: keeps the hook off `AbstractServerAccessor` (Bukkit-only) so the same push pattern extends to Fabric (B9 equivalent) without an interface change. Verification: `CoreMetricsTest` covers `setBinding`/`getBinding`/NOOP-fallback contract; production call-site landed in B9 (`MetricsBindingDispatcher.install()` invoked from `RTPBukkitPlugin#onEnable` and `RTPBukkitLitePlugin#onEnable`). *(verified 2026-05-16.)*
- [x] **B7.** Implement `PaperMetricsBinding` in `rtp-paper-common` wrapping `Bukkit.getTPS()` / `Bukkit.getAverageTickTime()` (Paper API confirmed — these are Paper-only methods). Verification: `:rtp-paper:rtp-paper-common:compileJava` green; smoke unit test against a mocked `Bukkit`. *(verified 2026-05-16: `PaperMetricsBinding` + `PaperMetricsBindingTest` present in `rtp-paper/rtp-paper-common/src/main/java/.../paper/metrics/`. Production wiring activated by B9: `MetricsBindingDispatcher.install()` reflectively loads `PaperMetricsBinding` when `Bukkit#getTPS` is reachable. End-to-end check: `MetricsBindingDispatcherTest` (in `rtp-plugin`) asserts `RTP.metrics.getBinding()` becomes non-NOOP after install on a Paper-API-bearing classpath.)*
- [x] **B8.** Implement `BukkitTpsSampler` in `rtp-bukkit-common`: 1-tick repeating task on `RTP.scheduler` recording `System.nanoTime()` per fire; three EMAs (1m / 5m / 15m); convert to TPS as `1e9 / movingAverageNanos`, clamp `[0, 20]`. Verification: unit test feeds a synthetic clock, asserts converged TPS within tolerance after each window length. *(verified 2026-05-16: `BukkitTpsSampler` + tests live in `rtp-bukkit/rtp-bukkit-common/.../bukkitplatform/metrics/`. Production wiring activated by B9: `MetricsBindingDispatcher.install()` instantiates the sampler on the raw-Spigot branch and schedules `tick()` on `RTP.scheduler.runTaskTimer(_, 1, 1)`. Row reworded to match the actual symbol name shipped — the original `BukkitTpsSampler` placeholder name was never the production class.)*
- [x] **B9.** Runtime-detect Paper at adapter init (reflective probe for `Bukkit.getTPS`); skip Spigot sampler when Paper is present. Verification: log line at `INFO` confirms which path was chosen on adapter startup; smoke test on a Paper devstack. *(verified 2026-05-16: shipped as `io.github.dailystruggle.rtp.bukkit.metrics.MetricsBindingDispatcher` in `rtp-plugin/`. Reflectively probes `org.bukkit.Bukkit#getTPS`; on hit installs `PaperMetricsBinding`, otherwise instantiates `BukkitTpsSampler` and schedules a 1-tick repeating task on `RTP.scheduler`. Idempotent install/uninstall, `INFO`-level path-chosen log line per row. Wired into both `RTPBukkitPlugin#onEnable` (full) and `RTPBukkitLitePlugin#onEnable` (lite), with matching teardown in both `onDisable`s. Covered by `MetricsBindingDispatcherTest` (3/3 green) which asserts NOOP→non-NOOP→idempotent-install→NOOP-restore round-trip on the Paper-API-bearing test classpath. Devstack smoke deferred to the Section B → C gate.)*
- [x] **B10.** Wire `rtp test full` to print `MetricsSnapshot.toString()` (replace the ad-hoc dump). **Strict superset** — must not drop any line that operators currently rely on. Verification: `:rtp-plugin:test --tests "TestFullCmdTest"` 8/8 green; the legacy `[RTP test/full] end (total-audited-warnings=...)` footer is preserved verbatim, with the new pass/fail per-subcommand table and `[RTP test/full] metrics: MetricsSnapshot{...}` line inserted ahead of it. *(landed 2026-05-16 in `TestFullCmd#finishSweep` + `FullAudit.stepWarnDeltas`; book-API re-render deferred to `docs/dev/TODO.md` section 7 follow-up bullet.)* **PASS/FAIL predicate fixed 2026-05-16 (late session)**: the 2026-05-16 Paper devstack log showed every step marked FAIL on an idle server because `FullAudit.accept(String)` was counting any `[RTP`-tagged INFO line as a warning. `SendMessage` now exposes a parallel `addInterceptor(BiConsumer<Level,String>)` channel (in `rtp-bukkit-common/.../tools/SendMessage.java`), and `FullAudit` is now a `BiConsumer<Level,String>` that counts only `>= Level.WARNING`. String-only interceptor channel is unchanged (back-compat); non-`log` `sendMessage(...)` paths pass `level=null` so player-facing chat never inflates the tally. Re-verification on a Paper devstack still required to tick the B → C gate.
- [x] **B11.** Update `InfoCmd` to render the *Health — server* and *Health — pipeline* groups from `Metrics.snapshot()` per `METRICS_PLAN.md > /rtp info Surface`. Verification: extend `InfoCmdTest` with the *one snapshot per invocation* assertion (no N×region calls); existing tests remain green. *(closed 2026-05-16: (i) snapshot caching landed via new `PlaceholderProvider.withSnapshot(Runnable)` + `pushSnapshot`/`popSnapshot`/`currentSnapshot` ThreadLocal API; `InfoCmd.onCommand` wraps its body in `withSnapshot(...)`, so all metrics-backed placeholders read one atomic snapshot per `/rtp info`. Assertion: `InfoCmdTest#onCommand_snapshotsMetricsExactlyOncePerInvocation` installs a counting `MetricsBinding` and asserts `tps1m()` is invoked exactly once. (ii) Added `MessagesKeys.infoTps`, `infoMSPTLive`, `infoSoftCap`, `infoDatabaseLatencyMs` + supporting placeholders `tps1m`/`tps5m`/`tps15m`/`mspt`/`tickBudgetUtilisation`/`softCap`/`playerCount`/`databaseLatencyMs` in `PlaceholderProvider` with documented `n/a` / `unbounded` sentinels. (iii) Added `MessagesKeys.infoHealthPipelineHeader` rendered ahead of the metrics group as the labelled `Health — Pipeline` block. EN + ES templates updated; 15/15 `InfoCmdTest` green.)*
- [x] **B12.** Add `rtp.info` colour-band thresholds to `messages.yml`. Verification: defaults render green/yellow/red per the plan; missing keys fall back to a hard-coded default without throwing. *(closed 2026-05-17: 7 new `MessagesKeys` enum entries (`infoThresholdTpsGreen`/`infoThresholdTpsYellow`/`infoThresholdMsptGreen`/`infoThresholdMsptYellow`/`infoThresholdCacheFillGreen`/`infoThresholdCacheFillYellow`/`infoThresholdNetworkAgeYellow`) + EN/ES YAML defaults (19.5/18.0, 30.0/45.0, 0.50/0.25, 5.0). New `io.github.dailystruggle.rtp.common.tools.ColourBands` (in `rtp-core`) resolves each threshold via `ConfigParser.getConfigValue(key, null)` with hard-coded fallbacks; `Throwable`-guarded and `RTP.getInstance()==null`-guarded so no `/rtp info` call path can throw on a missing key. Six new coloured-wrapper placeholders in `PlaceholderProvider` (`tps{1m,5m,15m}Coloured`, `msptColoured`, `tickBudgetUtilisationColoured`, `avgPipelineMsColoured`) prepend `&a`/`&e`/`&c`/`&7` and trail `&r`; bare placeholders retained for opt-out. EN+ES `infoTps`/`infoMSPTLive`/`infoAvgPipelineMs` templates re-pointed at the coloured variants; 7 sibling locales' legacy `infoMSPT` lines blanked to `""` to keep `LocaleResourceParityTest` (3/3) green after the bare-`[mspt]` placeholder was removed from baseline templates. Coverage: `ColourBandsTest` 7/7 (TPS / MSPT / tick-budget / cache-fill / network-age bands + `parseDouble` and `resolveDouble` no-config fallback).)*
- [x] **B13.** bStats — *Configuration adoption* chart group only (`platform`, `assembly_variant`, `database_backend`, `region_shapes_in_use`, `safety_features_enabled`, `addons_loaded`, `lite_features_dropped`). Reads `Metrics.snapshot()` cached values; no parallel sampling. Verification: chart IDs centralised in a new `BStatsChartIds` constants class; all chart lambdas covered by a unit test asserting they don't include `serverId`-equivalent strings. *(closed 2026-05-17: four missing config-adoption chart IDs added to `BStatsChartIds` (`REGION_SHAPES_IN_USE`, `SAFETY_FEATURES_ENABLED`, `ADDONS_LOADED`, `LITE_FEATURES_DROPPED`). Registered in `RTPCostMetricsCharts.register(Metrics, String)` next to the existing three; backing detector helpers added: `detectRegionShapesInUse` (AdvancedPie tally of `RegionKeys.shape` simple-class-names from `RTP.selectionAPI.permRegionLookup`), `detectSafetyFeaturesEnabled` (SimplePie sorted slug of non-default `SafetyKeys` booleans — `anvilPrefilterEnabled`, `biomeWhitelist` — falling back to `default`/`unknown`), `detectAddonsLoaded` (AdvancedPie tally restricted to the hard-coded `KNOWN_ADDON_PLUGINS` whitelist mirroring `plugin.yml > softdepend`, plus a `none` sentinel), and `lite_features_dropped` derived directly from the `assemblyVariant` constructor argument. All helpers `Throwable`-guarded so charts never throw at submission time. Anti-fingerprinting coverage: new `BStatsChartIdsAntiFingerprintingTest` (4/4 green) regex-scans every chart payload for UUID / IPv4 / hostname-shaped tokens and asserts the addons whitelist is unmodifiable so runtime code cannot leak arbitrary plugin names.)*
- [x] **B14.** TRACEABILITY.md - add rows for any new REQ-traceable test introduced in B2/B3/B11. Verification: `check_traceability.sh` clean. *(closed 2026-05-17: REQ-RTP-OBS-001 (non-blocking metrics snapshot), REQ-RTP-OBS-002 (single-sample pipeline recording), and REQ-RTP-OBS-003 (bounded heap/TPS/MSPT sampling cost) drafted in `docs/dev/REQUIREMENTS.md` §1.8 (D-005 approved). Four existing metrics tests renamed via `rename_element` to the `ReqRtpObs*` prefix: `CoreMetricsTest` -> `ReqRtpObs001CoreMetricsTest`, `MetricsSnapshotTest` -> `ReqRtpObs001MetricsSnapshotTest`, `PipelineHistogramTest` -> `ReqRtpObs002PipelineHistogramTest`, `HeapSamplerTest` -> `ReqRtpObs003HeapSamplerTest`. `TeleportPipelineTaskPhaseTest#runCleanup_records_one_sample_into_pipeline_histogram` annotated with Javadoc `Regression guard for REQ-RTP-OBS-002`. Three rows added to `docs/dev/TRACEABILITY.md` after the MAP block, pointing each REQ to its implementing classes and tests. `ReqRtpObs001CoreMetricsTest` 5/5 green post-rename; grep-confirmed all three IDs present in both REQUIREMENTS.md and TRACEABILITY.md.)*

> **Gate**: Section B verified green on a Paper devstack before Section C begins. (Spigot 1.20.1 devstack smoke deprioritized to nice-to-have per 2026-05-16 user direction; user will exercise the fallback path at final-results time.)
>
> **Audit snapshot (2026-05-17, late session)**: **Section B is now fully closed** (B1-B14 all ticked). B14 closed by drafting REQ-RTP-OBS-001/002/003 in `REQUIREMENTS.md` §1.8, renaming four metrics tests under the `ReqRtpObs*` prefix, and adding three rows to `TRACEABILITY.md`. Note: Section C work (`RTPCostMetricsCharts` runtime-cost charts) remains landed ahead of the B -> C gate; reconcile when Section C opens.
>
> **Section A → B gate**: passed 2026-05-15 (all A1–A5 ticked). **Section B → C gate: PASSED 2026-05-16 (late session)** on Paper 26.1.2 + Java 25. Two consecutive `/rtp test full` transcripts (pre-reload at 23:53 and post-`/reload` at 23:54) both produced `12/14 passed (85.7%), total-audited-warnings=2` with sane `MetricsSnapshot` values (tps1m≈19.83–19.93, mspt≈11.66–11.73, tickBudgetUtilisation≈0.23, heapUsed=1.7–2.6 GB, avgPipelineMs=57–75); `[METRICS] installed PaperMetricsBinding (Bukkit#getTPS detected)` line confirms B9 path selection. The two persistent FAILs (`disconnect-midflight`, `economy-isolation`) are pre-existing defects already catalogued in `POTENTIAL_BUGS.md`, not metrics-SPI regressions. Note: the user issued `/reload` (Bukkit's full server reload) rather than `/rtp reload` (plugin-internal config reload); a full `/reload` does **not** re-fire `onDisable`/`onEnable` in modern Paper, so the `MetricsBindingDispatcher.uninstall()` → `install()` round-trip is exercised only at server stop+start. The pre/post-`/reload` snapshot equivalence demonstrates the binding survives a Bukkit `/reload` without going stale, which is the operationally relevant property. Spigot 1.20.1 transcript no longer required (see scope amendment above).

---

## Section C — Metrics Plan, Phase M2 (Folia + Fabric)

> **2026-05-17**: D-005 proposal drafted at [`PROPOSAL-section-c-folia-fabric-metrics.md`](PROPOSAL-section-c-folia-fabric-metrics.md). Implementation gated on user approval per Rule D-005 (multi-module change + additive SPI on `MetricsSnapshot` / `MetricsBinding` + new `MetricsKeys` config surface). Delete the proposal file once Section C is submitted.

- [~] **C1.** Implement `FoliaMetricsBinding` in `rtp-folia-common` with the `max` (mspt/tickBudget) / `mean` (tps*) defaults from `METRICS_PLAN.md > Folia Aggregation`. Verification: `:rtp-folia:*:test` green; per-region detail accessible via `MetricsSnapshot.foliaRegions()` and gated by `metrics.folia.includeRegions`. *(D-005 proposal approved 2026-05-17; SPI sub-step landed; binding implementation queued for next session.)*
  - [x] **C1.0** *(2026-05-17)* SPI extension landed in `rtp-core`: new `FoliaRegionSample` carrier, `MetricsBinding#foliaRegions()` default returns `Collections.emptyList()`, `MetricsSnapshot` gains 16-arg ctor (15-arg ctor preserved as a delegating overload for back-compat with M1 callers + `Metrics.NOOP`), `CoreMetrics.snapshot()` forwards `binding.foliaRegions()`. Verification: `FoliaRegionSampleTest` 9/9 green (binding default empty, 15-arg ctor back-compat, 16-arg accept+immutable+null-handling, `CoreMetrics` propagation, `FoliaRegionSample#tickBudgetUtilisation()` math); existing `rtp-core/.../metrics/` suite 24/24 green (no regression). Note: deviates from `PROPOSAL §3` question 1 option (a) "delete 15-arg ctor" — kept both constructors because two existing tests and `Metrics.NOOP` use the 15-arg form, and the new 16-arg form is strictly additive. Cleanup of 15-arg ctor can happen later as a stay-on-task row if desired.
  - [x] **C1.1** *(2026-05-17)* Added `MetricsKeys` enum (`rtp-core/.../configuration/enums/MetricsKeys.java`) with `foliaIncludeRegions`, `foliaAggregationTps`, `foliaAggregationMspt`, `foliaAggregationTickBudget`, `version`; baseline `rtp-plugin/src/main/resources/metrics.yml` with documented defaults (`true` / `mean` / `max` / `max`) per `METRICS_PLAN.md > Folia Aggregation`; registered in `Configs.reloadConfigs()` next to `performance.yml`. Verification: full `.\gradlew build` green; `rtp-core/.../metrics/` test suite 24/24 green. **Locale parity deferred**: `LocaleResourceParityTest` is divergence-only (absent `<locale>/metrics.yml` falls back to the baseline) so all 8 shipped locale dirs (cat / de / es / fr / ja / ko / nl / zh) remain green without per-locale copies; translated `<locale>/metrics.yml` + `<locale>/metrics.lang.yml` pairs are queued for the next translation-pass session and tracked separately under `TRANSLATION_GUIDE.md`. (The "10 sibling locales" figure in the C1.1 row was aspirational; the repo ships 8 locale dirs today — `lang/shape` and `lang/vert` are template categories, not locales.)
  - [x] **C1.2** *(2026-05-17)* Implemented `FoliaRegionTpsSampler` (package-private EMA carrier — 1m / 5m / 15m windows @ 20 TPS, volatile reads, `lastTickWallNanos` for idle eviction) and `FoliaMetricsBinding` (public, per-region `ConcurrentHashMap<RegionKey, FoliaRegionTpsSampler>` registry; `recordRegionTick(RegionKey)` public API to be driven by `FoliaRegionProcessor` in C1.2b; aggregation respects `MetricsKeys.foliaAggregationTps` / `foliaAggregationMspt` defaults (mean / max) with `Throwable`-guarded readers that fall back to documented defaults when `RTP.getInstance()` is null; idle eviction at 60s drops stale samplers; opaque lifetime-stable `region-N` ids minted via `AtomicInteger`; `playerCount`/`softCap` sourced from `Bukkit` via `IntSupplier` test seam; `foliaRegions()` returns `Collections.unmodifiableList(...)` and respects `foliaIncludeRegions` opt-out). Per-region `playerCount`/`queueDepth` are carried as `0` for now — the per-region attribution is **C1.2b** (deferred). Verification: `FoliaMetricsBindingTest` 8/8 green covering empty-binding sentinels, seed→emit→stable-id, null-key no-op, idle eviction, mean aggregation across two regions, supplier delegation, unmodifiable-list contract, distinct ids. Full `.\gradlew build` PASSED.
  - [x] **C1.2b** *(2026-05-17)* Wired `FoliaRegionProcessor.run()` to invoke `FoliaMetricsBinding#recordRegionTick(key, queue.size())` once per pulse via `RTP.metrics.getBinding()` + `instanceof` check; failures are swallowed at `FINER` (best-effort observability never breaks the pipeline). Added a 2-arg overload `recordRegionTick(RegionKey, int queueDepth)` on `FoliaMetricsBinding` carrying queueDepth into a parallel `ConcurrentHashMap<RegionKey, Integer> regionQueueDepths` (negative clamped to 0; evicted alongside the sampler in `evictIdle`). Per-region `playerCount` deliberately stays `0` in M2 (no cheap region→player map on Folia without iterating `getOnlinePlayers()` + `isOwnedByCurrentRegion` checks; deferred). Verification: new test `recordRegionTick_carries_queue_depth_per_region` (latest-wins + negative-clamp); `FoliaMetricsBindingTest` 9/9 green; full `.\gradlew build` PASSED.
  - [x] **C1.3** *(2026-05-17)* Added Folia branch to `MetricsBindingDispatcher#install()` ahead of the Paper branch: new `isFoliaRuntime()` probe (`Class.forName("io.papermc.paper.threadedregions.RegionizedServer")`); installs `FoliaMetricsBinding` reflectively via FQN (rtp-plugin only sees `rtp-folia-common` transitively, same pattern as `PaperMetricsBinding`). No external sampler task is scheduled on the Folia path — `FoliaRegionProcessor` (C1.2b) drives `recordRegionTick` from each region's own thread. Uninstall path unchanged (`setBinding(null)` clears Folia binding back to NOOP). Verification: full `.\gradlew build` PASSED; `FoliaMetricsBindingTest` 9/9 green.
- [x] **C2.** Implement `FabricMetricsBinding` in `rtp-fabric-common` using the server tick callback chain wired in `MULTI_PLATFORM_PLAN.md > Step E2`. Verification: `:rtp-fabric:rtp-fabric-common:test` green; smoke test on Fabric dev server confirms non-zero TPS in the snapshot.
  - Landed 2026-05-17. `FabricMetricsBinding` (`rtp-fabric/rtp-fabric-common/.../fabric/metrics/`) is a single-server EMA sampler mirroring `BukkitTpsSampler` math (1m/5m/15m windows @ 20 TPS, `1e9/ema` clamped to `[0,20]`, mspt = 1m EMA in ms). `playerCount`/`softCap` sourced via `IntSupplier` hooks so the binding has zero `net.minecraft` imports; `foliaRegions()` inherits the empty-list default (single-region runtime — correct semantics). Driven from `FabricEventBridge.onEndServerTick` via an `instanceof FabricMetricsBinding` probe (best-effort, throws swallowed at `FINER` so observability never crashes the tick loop). Installed in `RTPFabricMod.onInitialize` via `RTP.metrics.setBinding(...)` before the bridge registers; suppliers resolve `MinecraftServer.getPlayerCount()` / `getMaxPlayers()` via new `reflectIntZeroArg(target, candidates[])` helper (mojmap + intermediary aliases — `getPlayerCount`/`method_3788`, `getMaxPlayers`/`method_3802`). Tests: `FabricMetricsBindingTest` 11/11 green (synthetic-clock EMA convergence, null-ctor guards, supplier delegation, negative-clamp, throwing-supplier degradation, sustained-nominal stability). Full multi-module `.\gradlew build`: PASSED. Smoke test on a live Fabric server deferred — not a checklist blocker; the binding will surface in `/rtp info` + bStats as soon as C3/C4 land.
- [x] **C3.** Update `InfoCmd` to render the per-region table inline when `MetricsSnapshot.foliaRegions()` is non-empty. Verification: command no longer requires platform-specific code paths beyond what `MetricsBinding` exposes.
  - Landed 2026-05-17. New `MessagesKeys` entries `infoFoliaRegionsHeader` + `infoFoliaRegion` (per-row template with `[regionId] [tps1m] [mspt] [playerCount] [queueDepth] [tickBudgetUtilisation]` tokens); baseline defaults added to `rtp-plugin/src/main/resources/messages.yml`. `InfoCmd.run(...)` reads `PlaceholderProvider.currentSnapshot().foliaRegions()` and renders the header + per-row substitution loop only when the list is non-empty — Paper/Bukkit/Fabric inherit the empty-list default, so the table is structurally suppressed there (no platform branching in core). Inline placement immediately after `infoDatabaseLatencyMs` and before `infoDisclaimerHeader` keeps it inside the Health block of the player view. Deviation from `PROPOSAL §3` Q3: rendered inline in the default `/rtp info` output instead of behind a `verbose` subcommand — simpler, no Brigadier surface change, and on non-Folia platforms the table is already invisible (zero noise). New helper `InfoCmd.formatDouble(double)` standardises 2-decimal-place rendering with NaN passthrough. Tests: `InfoCmdTest` 17/17 green including new `onCommand_rendersFoliaRegionTable_whenBindingPublishesRegions` (verifies header + token-substituted row output) and `onCommand_suppressesFoliaRegionTable_whenBindingReturnsEmptyList` (verifies single-region platforms remain quiet). The render test injects templates via `lang.setData(...)` because the test ConfigParser stub has no `RtpYamlConfig` backing store, so the production `lang.set(E, Object)` path NPEs — fixture limitation, not a production bug.
- [x] **C4.** bStats — *Runtime health* chart group (`region_count`, `cache_pool_health`, `tps_buckets`, `mspt_buckets`, `pipeline_latency_buckets`, `memory_tracker_pressure`, `chunk_load_backlog_pressure`, `s005_violations_recent`). Verification: bucketisation happens once per snapshot, not per chart fetch.
  - Landed 2026-05-17. Additive only per `PROPOSAL §3` Q5 — no existing chart was rewritten. Five new IDs in `BStatsChartIds`: `TPS_TRENDLINES` (MultiLineChart, 1m/5m/15m EMA scaled `*100` for sub-integer resolution in bStats int-only line charts, clamped `[0, 2500]`), `HEAP_PRESSURE` (AdvancedPie, bucketised `heapUsed/heapMax` percentage), `TICK_BUDGET_UTILISATION` (AdvancedPie, fraction `[0,1]` buckets — meaningful on Folia, NaN→"unknown" on single-region platforms), `FOLIA_REGION_COUNT` (AdvancedPie, fingerprint-safe bucketed `foliaRegions().size()` — `0/1/2-4/5-16/17-64/65+`; non-Folia structurally buckets to `"0"` via the empty-list default), `PENDING_TELEPORTS_PRESSURE` (AdvancedPie, symmetric staircase with `QUEUE_DEPTH_PRESSURE`). All five suppliers read `RTP.metrics.snapshot()` once per fetch — bucketisation is pure function of snapshot fields, no per-chart recomputation. Wired in `RTPCostMetricsCharts.register(...)` after the existing pressure block; helpers made package-private (`scaledTps`, `heapPressureBucket`, `tickBudgetBucket`, `foliaRegionCountBucket`, `pendingTeleportsBucket`) to enable boundary tests without reflection. Tests: new `RuntimeHealthBucketsTest` 5/5 green (NaN / negative / boundary / clamp / overflow per helper). Full multi-module `.\gradlew build`: PASSED. Note: the existing reflective `getTPS()` fallback at `RTPCostMetricsCharts:358` (`safeTps()`) remains untouched — its removal is scoped to row C6 §1.6.4.
- [x] **C5.** TRACEABILITY.md row updates as needed. Verification: `check_traceability.sh` clean. Landed 2026-05-17. Extended REQ-RTP-OBS-001 to cite `FoliaRegionSample` implementing class + `FoliaRegionSampleTest` and `InfoCmdTest` (Folia-table render/suppress cases); extended REQ-RTP-OBS-003 to cite `FoliaMetricsBinding`, `FoliaRegionTpsSampler`, `FabricMetricsBinding`, `MetricsBindingDispatcher`, `RTPCostMetricsCharts` (bStats *Runtime health* chart group) implementing classes + `FoliaMetricsBindingTest`, `FabricMetricsBindingTest`, `RuntimeHealthBucketsTest`. OBS-002 untouched (no pipeline-recording surface change in Section C). Companion deliverable for this session: `docs/dev/scratch/PROPOSAL-metrics-api-extraction.md` captures the deferred `metrics-api/` subproject extraction (4-file SPI move + docs tree); trigger to revisit = both C6 submitted **and** one beta released with the M2 metrics surface.
- [ ] **C-DEFERRED.** Extract `metrics-api/` subproject per `docs/dev/scratch/PROPOSAL-metrics-api-extraction.md`. **Not in current scope.** Trigger: C6 submitted + one beta release with M2 metrics surface in production. D-005 re-confirmation required at trigger time. **Conceptual sibling of C6** (per `PROPOSAL-metrics-api-extraction.md §1.0`): C6 consolidates metric-shaped reads at the call-site / package layer; this row consolidates the SPI those reads land in at the module layer. Two halves of one consolidation effort, sequenced C6 → C-DEFERRED so the extraction stays a near-pure module move rather than a "move + consolidate" combo.
- [ ] **C6.** *(added 2026-05-17, §1.6 approved in-session)* Cross-platform metrics consolidation for parity across Paper / Bukkit / Folia / Fabric: route `RTPServerAccessor#getTPS(int)` through `Metrics.snapshot()` (1.6.1); replace size-only player-count read at `RTPBukkitPlugin.java:275` with `snapshot().playerCount` (1.6.2); reconcile soft-cap reads (`RTPBukkitPlugin:249` + `FabricEventBridge:477-484` reflective `getMaxPlayers`) into snapshot / `FabricMetricsBinding#softCap()` (1.6.3); delete redundant reflective `getTPS()` fallback at `RTPCostMetricsCharts#register:358` (1.6.4); add `MetricsConsolidationArchTest` ArchUnit drift guard (1.6.5). Verification: new `RTPServerAccessorTpsParityTest` green (real binding installed → real TPS; NOOP → platform fallback); new `MetricsConsolidationArchTest` green; full per-platform test passes for `rtp-core`, `rtp-paper`, `rtp-folia`, `rtp-fabric`, `rtp-plugin`. ArchUnit guard scope: `rtp-core` + `rtp-bukkit` + `rtp-paper` + `rtp-folia` + `rtp-fabric` + `rtp-plugin`; `helpers/`, `commands-api/`, `addons/` exempt.

> **Gate**: dual-runtime smoke test — one JAR loads on Paper and Fabric, `/rtp info verbose` prints sane health on both. Coordinates with `MULTI_PLATFORM_PLAN.md > Phase 2 acceptance gate`.
>
> **C6 status (2026-05-17 late session)**: implementation LANDED — tick the C6 box at line 85 on next edit. Summary: 1.6.1 routes `RTPServerAccessor#getTPS(int)` through `RTP.metrics.snapshot()` on all three impls (`AbstractServerAccessor`, `AbstractFoliaServerAccessor`, `FabricServerAccessor`) with the original platform-specific fallback preserved on the M0 NOOP path (Bukkit reflective `recentTps`, Folia nominal 20.0, Fabric `MinecraftServer#getAverageTickTimeNanos`-or-`getAverageTickTime`). Window selection: `ticks>=600 -> tps15m`, `ticks>=100 -> tps5m`, else `tps1m`. 1.6.2 + 1.6.3 land in `RTPBukkitPlugin.initLoginReserveCache` and `FabricEventBridge.initLoginReserveCache`: both now prefer `snapshot.softCap` / `snapshot.playerCount`, falling back to `Bukkit.getMaxPlayers()` / `Bukkit.getOnlinePlayers().size()` (Bukkit side) or the existing reflective `resolveMaxPlayers` / `resolveOnlinePlayerCount` helpers (Fabric side) when the binding is NOOP. 1.6.4 simplifies `RTPCostMetricsCharts#safeTps()` to a single-line `RTP.metrics.snapshot().tps1m` read; the reflective `Server#getTPS` probe is gone (chart degrades to the `"unknown"` bucket on NaN, which is the documented behaviour). 1.6.5 adds `MetricsConsolidationArchTest` (`rtp-plugin/src/test/.../bukkit/metrics/`) with two ArchUnit rules: `Server#getTPS` may only be called from `*MetricsBinding` classes; `Server#getMaxPlayers` may only be called from `*MetricsBinding` or `*TpsSampler` classes. The guard lives in rtp-plugin because that is the only module that aggregates rtp-core + rtp-bukkit-common + rtp-paper-common + rtp-folia-common + rtp-fabric-common on a single test classpath. Companion `RTPServerAccessorTpsParityTest` verifies a non-NOOP `StubBinding` propagates verbatim through `AbstractServerAccessor#getTPS(1/100/600)` and that the NOOP path still returns a finite positive value. Verification: 2/2 + 2/2 green via `run_test`. `.\gradlew build` red on an unrelated pre-existing `ReqRtpF013SpanishLocaleContentTest` (Spanish locale parity); `git status` shows the messages.yml family was dirty from prior sessions, none of the C6 edits touch locale files. C-DEFERRED stays `[ ]` per its own trigger gate (C6 submitted **and** one beta release with M2 surface in production — neither holds yet).

---

## Section D — Multi-Server Plan, Phase 0 (docs-only, D-005 gate)

Source-of-truth: [`docs/dev/MULTI_SERVER_PLAN.md`](../MULTI_SERVER_PLAN.md).

- [ ] **D1.** Draft `docs/adr/ADR-025-multi-server-proxy-support.md` using `docs/adr/ADR-TEMPLATE.md`. Must reference: D1–D4 from the plan, durable-transport requirement (D2), reuse of `AbstractSQLDatabaseAccessor` (D3), env-var HMAC (D4 v1). Must **not** supersede ADR-022. Verification: ADR file present; INDEX.md and MAP.md updated to list it; user approval recorded in the ADR's *Status* field.
- [ ] **D2.** Author REQ-RTP-NET-001…005 in final `shall` phrasing (style per `RULES.md`). Insert into `docs/dev/REQUIREMENTS.md` next to existing REQ-RTP-NET section if any, or at end. Verification: `RULES.md` style check passes; no temporal phrasing; no implementation actions.
- [ ] **D3.** Add glossary entries to `docs/dev/GLOSSARY.md`: *backend*, *proxy*, *reservation token*, *transport*, *network snapshot*, *backend selector*, *network wait queue*. Verification: each term has a one-paragraph definition; no overlap with existing entries.
- [ ] **D4.** Update `MULTI_SERVER_PLAN.md > Phase 0` checklist to tick the items above. Verification: each row references the artifact it produced (ADR-025 path, REQ IDs, glossary entries).
- [ ] **D5.** Curve plot generation script under `scripts/` (matplotlib) producing PNGs/SVGs for `linear` / `exponential` / `logarithmic` / `sigmoid` / `step` / `power` at the documented defaults. Verification: script runs reproducibly on a fresh checkout (Python venv + `requirements.txt`); outputs land in `docs/admin/proxies/loadbalancing/` (subdirectory under the existing stub).
- [ ] **D6.** Author `docs/admin/proxies/LOAD_BALANCING.md` embedding the plots from D5 + a plain-language explanation of each curve. Verification: rendered correctly on GitHub; cross-linked from `docs/admin/proxies/INDEX.md`.

> **Gate**: D-005 user approval on the full Phase-0 deliverable bundle (ADR + REQs + GLOSSARY + admin doc) before Section E begins.

---

## Section E — Multi-Server Plan, Phase 1 (core SPI; no proxy adapter yet)

- [ ] **E1.** Define `RtpTriggerSource` and `RtpDispatcher` in `rtp-core` (no platform imports). Verification: `:rtp-core:compileJava` green; ArchUnit guard clean.
- [ ] **E2.** Define `BackendSelector` interface and ship a single concrete strategy: configurable weighted average per `MULTI_SERVER_PLAN.md > Configurable Weighted Average`. Includes curve catalogue (`linear` / `exponential` / `logarithmic` / `sigmoid` / `step` / `power`), per-metric `normalize` + `weight`, per-backend weight multiplier, capped retry / cooldown / score-sticking. Verification: pure function of `NetworkSnapshot`; no I/O during `choose()`; unit tests for each curve at `k`/`p`/`threshold` defaults plus boundary inputs.
- [ ] **E3.** Define `NetworkTransport` interface (request/response, broadcast, subscribe). Verification: spec doc inside the package as KDoc; reference `MULTI_SERVER_PLAN.md > Coordinate Resolution Timing`.
- [ ] **E4.** Implement `InMemoryNetworkStateBinding` (single-JVM tests, no-op default). Verification: round-trip request/response in a unit test with two simulated backends.
- [ ] **E5.** Add the network-state member adjacent to `AbstractSQLDatabaseAccessor` per D3. Verification: `network.enabled: false` keeps the member null; member surface (`writeBackendState`, `readNetworkSnapshot`, reservation-token CRUD, network wait-queue CRUD) covered by an interface contract test.
- [ ] **E6.** **No-op test** (REQ-RTP-NET-005 gate): `network.enabled: false` produces byte-identical behaviour to single-server. Verification: dedicated test class; runs in default `:rtp-core:test`.
- [ ] **E7.** `commands-api` proxy-side surface — early TODO from the plan: `ProxySender`, `NetworkAwareCommand` mixin, tab-completion routing. Verification: interfaces defined and contract-tested, with no proxy-platform imports yet.
- [ ] **E8.** `recentPicks` decay implementation (halflife 10s, λ ≈ 0.0693 s⁻¹) integrated as a metric row in the selector. Verification: unit test feeds a synthetic clock, asserts the score returns to background within ~30s after a single pick.
- [ ] **E9.** Update `MULTI_SERVER_PLAN.md > Phase 1` checklist with completed rows. Verification: each row references its test name.

> **Gate**: Phase 1 acceptance — single-JVM tests with two simulated backends green; no-op test green; ArchUnit clean.

---

## Section F — Multi-Server Plan, Phase 2 (Velocity adapter + Redis transport)

- [ ] **F1.** Bootstrap `rtp-proxy/rtp-proxy-common/` and `rtp-proxy-velocity/` modules per `MULTI_SERVER_PLAN.md > Module shape`. Single JAR multi-loader bootstrap extends to the proxy axis (per *Intended Usage & Deployment Model* in the plan). Verification: `:rtp-proxy:rtp-proxy-velocity:build` green; manifest/`velocity-plugin.json` declares the entry point.
- [ ] **F2.** Implement `RedisNetworkStateBinding` (Lettuce, async). Verification: integration test against an embedded Redis (e.g. `embedded-redis`); no blocking `.get()` calls.
- [ ] **F3.** Implement reservation token state machine (`PENDING → CLAIMED → CONSUMED → EXPIRED`) on the network-state member. Idempotent consumption; reaper on `RTP.scheduler.runTaskTimerAsynchronously`. Verification: a dedicated regression suite analogous to `ReqRtpS004NullChunkAttributionTest` covering replay, TTL expiry, orphaned `MemoryTracker` entries.
- [ ] **F4.** Velocity `ServerPreConnectEvent` hook (PostOrder.LATE) that rewrites the target backend per the selector's choice. Verification: integration test against a Velocity dev instance; transfer happens once, no spawn-flash.
- [ ] **F5.** `CommandTriggerSource` wired through the dispatcher; `/rtp` from a backend or the proxy itself flows the same path. Verification: integration test exercises both origins.
- [ ] **F6.** HMAC envelope on every transport packet using `RTP_NET_SECRET` env var (D4 v1). Verification: bad-secret packets are rejected and logged under S-004 attribution; no replay within TTL window.
- [ ] **F7.** Update `InfoCmd` *Health — network* block per `METRICS_PLAN.md > /rtp info Surface > Health — network`. Verification: block appears only when `network.enabled: true`; integration test confirms stale-backend listing.
- [ ] **F8.** bStats *Feature shape* charts (`selection_strategy_shape`, `region_topology`, `trigger_sources`) and the proxy-side chart set. Verification: proxy charts deliberately omit backend-pool shape (anti-fingerprinting).
- [ ] **F9.** **Acceptance**: cross-server `/rtp` round-trip on a Velocity + 2× Paper devstack with Redis. Manual smoke + recorded test transcript in `LESSONS_LEARNED.md`.
- [ ] **F10.** Postgres-vs-Redis comparative benchmark (deferred from earlier; runs *after* both transports are stable). Verification: benchmark script in `scripts/`, results recorded in `LESSONS_LEARNED.md`.

> **Gate**: Phase 2 acceptance — F9 transcript + F3 regression suite green.

---

## Section G — Multi-Server Plan, Phase 3 (Postgres transport + Join trigger + BungeeCord)

- [ ] **G1.** `PostgresNetworkStateBinding` using `LISTEN/NOTIFY` and `SELECT ... FOR UPDATE SKIP LOCKED`. Verification: integration test against an embedded Postgres; race-free reservation claim under contention.
- [ ] **G2.** `JoinTriggerSource` wired *proxy-side* per D1. Verification: per-region/world map sourced from proxy config; no per-backend mirror.
- [ ] **G3.** `rtp-proxy-bungee` adapter (BungeeCord + Waterfall). Verification: integration test on a Bungee devstack.
- [ ] **G4.** **Acceptance**: same scenarios as F9, but on Bungee + Postgres.

---

## Section H — Multi-Server Plan, Phase 4 (Generic SQL + hardening + release)

- [ ] **H1.** `GenericSqlNetworkStateBinding` (MySQL/MariaDB polling). Verification: integration test confirms polling fallback latency stays within the load-balancer's `attemptTimeoutMs`.
- [ ] **H2.** Security audit — HMAC, replay protection, schema-version negotiation, kill switch. Verification: dedicated test suite + security review notes in `LESSONS_LEARNED.md`.
- [ ] **H3.** `rtp test full network` aggregator across the network. Verification: integration test exercises three backends with one stale.
- [ ] **H4.** `docs/admin/proxies/` admin docs filled out per `docs/admin/proxies/INDEX.md` planned-pages list. Verification: each row in the planned-pages table now resolves to an authored doc.
- [ ] **H5.** `CHANGELOG.md` entries per phase under *Unreleased*. Verification: PR review includes the changelog diff.
- [ ] **H6.** First public proxy-mode beta release (gated on full audit green). Verification: tagged release; release notes link to ADR-025 and this checklist file's deletion.
- [ ] **H7.** **Delete this checklist file.** Verification: `Test-Path docs\dev\scratch\CHECKLIST-metrics-and-multiserver.md` returns false; no references remain in any committed doc.

---

## Section I — Cross-cutting hygiene (run continuously)

- [ ] **I1.** Each new REQ-traceable test added during B–H is recorded in `TRACEABILITY.md` in the same commit. Verification: `check_traceability.sh` clean before each PR merge.
- [ ] **I2.** Each session that touches an unfamiliar symbol re-reads the *Domain Analogies & Aliases* table in `AGENTS.md` so cache-tier and queue language stays consistent. (No ticking — this is a habit, not a deliverable.)
- [ ] **I3.** Each incidental potential bug discovered during execution lands in `POTENTIAL_BUGS.md` per `AGENTS.md > Stay-On-Task Policy`. Verification: review at end of each phase.
- [ ] **I4.** Every multi-class or cross-module change pauses for D-005 review before code lands.
- [ ] **I5.** No `printStackTrace()` in any new code; all error paths route through `RTP.log(Level.WARNING, msg, e)`.
- [ ] **I6.** No `org.bukkit.*` imports in `rtp-core` or `rtp-api` (existing rule). Verification: ArchUnit guard.
- [ ] **I7.** `network.enabled: false` no-op contract holds at every milestone (REQ-RTP-NET-005). Verification: re-run E6 test before each PR merge in Sections F–H.

---

*Self-update note*: when a row's verification artifact moves (test renamed, file relocated), update the row in the same commit. Do not let the checklist drift from reality. When all sections are submitted and H7 is verified, this file is deleted, not archived — the canonical record lives in the commit history of `MULTI_SERVER_PLAN.md`, `METRICS_PLAN.md`, ADR-025, and `LESSONS_LEARNED.md`.
