# --- RTP Performance Settings ---
# Documentation: https://github.com/DailyStruggle/RTP/blob/master/docs/admin/PERFORMANCE.md

maxAttempts: 32 # Max search attempts before giving up
viewDistanceSelect: 0 # Chunk radius to load during selection
viewDistanceTeleport: 0 # Chunk radius to load before teleport
period: 20 # Ticks between background cache cycles
syncAllottedTime: 50 # Max ms per tick for sync tasks
asyncAllottedTime: 50 # Max ms per tick for async tasks
minTPS: 19.0 # Min TPS to continue background queueing
postTeleportQueueing: false # Refill cache immediately after teleport
syncLoading: false # Use sync chunk loading (NOT RECOMMENDED)
onEventParsing: false # Parse permissions on every event
effectParsing: true # Parse effect permissions on startup
biomeRecall: true # Reuse found biome locations
biomeRecallForced: false # Only use biomes already in cache
checkOnChunkLoads: false # Check for safe spots in all loaded chunks
visitorEnabled: false # Enable background data gathering
loginCacheEnabled: false # Maintain a reserved cache of locations for join-time teleports (rtp.onevent.firstjoin / rtp.onevent.join). Sized by remaining empty player slots.
loginCacheCap: 0 # Hard cap for the login cache. 0 = auto (server max-players, snapshotted at startup).
pregeneratedPreference: 0.0 # Probability [0.0, 1.0] of rejecting an ungenerated chunk per selection attempt. 0.0 = no bias (default), 1.0 = strictly avoid ungenerated land. Generated chunks always pass.
backlogRefillThreshold: 0.5 # Fraction [0.0, 1.0] of L3 backlog capacity at which refill resumes (hysteresis). 0.5 = refill only after L3 drains to 50% (default). 1.0 = refill continuously. Higher values group .mca-bin checks for better CPU/memory efficiency.

# DO NOT TOUCH VERSION NUMBER
version: "1.0"