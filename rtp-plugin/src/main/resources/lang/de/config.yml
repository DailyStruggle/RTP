# Wartezeit (Sekunden) vor der Teleportation
# @type: integer
# @range: [0, null]
# @unit: seconds
# @default: 2
teleportVerzoegerung: 2

# Maximale Bewegungsdistanz während der Verzögerung vor Abbruch
# @type: integer
# @range: [0, null]
# @unit: blocks
# @default: 2
distanzAbbruch: 2

# Wartezeit (Sekunden) zwischen erfolgreichen Nutzungen
# @type: integer
# @range: [0, null]
# @unit: seconds
# @default: 300
teleportAbkuehlung: 300

# Maximum number of successful /rtp uses a player may make within the
# lockAfterResetSeconds window before being locked out (BetterRTP LockAfter parity).
# 0 disables the cap. Bypassed by rtp.nolock.
# @type: integer
# @range: [0, null]
# @default: 0
lockAfterUses: 0

# Length of the rolling usage-cap window for lockAfterUses. The counter resets this
# many seconds after the window's first use. 0 means the cap never resets (a hard
# lifetime cap).
# @type: integer
# @range: [0, null]
# @unit: seconds
# @default: 0
lockAfterResetSeconds: 0

# When true, a successful /rtp sets the landed location as the player's persistent
# spawn anchor (BetterRTP SetAsRespawn parity).
# @type: boolean
# @default: false
setRespawnOnTeleport: false

# Befehle, die nach der Teleportation ausgeführt werden
# @type: list<string>
konsolenBefehle:
  - "say [player] hat sich gerade zufällig teleportiert!"

# Befehle, die als der teleportierte Spieler bei erfolgreichem Teleport ausgeführt werden.
# Verwenden Sie [player] für den Spielernamen. Leere Einträge werden ignoriert.
# @type: list<string>
spielerBefehle:
  # Befehle, die als der teleportierte Spieler bei erfolgreichem Teleport ausgeführt werden.
  # Verwenden Sie [player] für den Spielernamen. Leere Einträge werden ignoriert.
  # @type: list<string>
  - ""

# Datenbankkonfiguration
datenbank:
  # Backend-Typ.
  # @type: enum
  # @options: ["yaml", "sqlite", "mysql", "postgresql"]
  # @default: "sqlite"
  type: "sqlite"
  # Datenbank-Host. Wird nur von mysql/postgresql verwendet.
  # @type: string
  host: "127.0.0.1"
  # Datenbank-Port. Wird nur von mysql/postgresql verwendet.
  # @type: integer
  # @range: [1, 65535]
  port: 3306
  # Datenbank- / Schema-Name. Wird nur von mysql/postgresql verwendet.
  # @type: string
  name: "rtp"
  # Datenbank-Benutzername. Wird nur von mysql/postgresql verwendet.
  # @type: string
  username: "root"
  # Datenbank-Passwort. Wird nur von mysql/postgresql verwendet.
  # @type: string
  password: "password"

# Netzwerk- und Synchronisationskonfiguration
netzwerk:
  redis:
    # Enable Redis-backed network state binding.
    # @type: boolean
    # @default: false
    enabled: false
    # Redis host.
    # @type: string
    host: "127.0.0.1"
    # Redis port.
    # @type: integer
    # @range: [1, 65535]
    port: 6379
    # Redis password. Empty string disables auth.
    # @type: string
    password: ""

# Allgemeines Menü-Framework.
# `renderer` ist eine geordnete Präferenzliste von Renderer-IDs. Bei einer
# Ausnahme oder einem fehlenden Adapter geht das Framework die Liste durch
# und greift auf den nächsten Eintrag zurück. Ist die Liste erschöpft,
# greift der Pfad ohne Token auf die konfigurierbare `menuInvalid`-Meldung
# zurück. Verfügbare IDs: book. (Der Chat-Renderer kommt im Folge-Release
# und wird dann der Standardliste hinzugefügt.)
menu:
  # Geordnete Präferenzliste von Renderer-IDs.
  # @type: list<enum>
  # @options: ["book", "chat"]
  renderer:
    # Allgemeines Menü-Framework.
    # `renderer` ist eine geordnete Präferenzliste von Renderer-IDs. Bei einer
    # Ausnahme oder einem fehlenden Adapter geht das Framework die Liste durch
    # und greift auf den nächsten Eintrag zurück. Ist die Liste erschöpft,
    # greift der Pfad ohne Token auf die konfigurierbare `menuInvalid`-Meldung
    # zurück. Verfügbare IDs: book. (Der Chat-Renderer kommt im Folge-Release
    # und wird dann der Standardliste hinzugefügt.)
    - "book"

# ÄNDERE NICHT DIE VERSIONSNUMMER
version: 3.0
