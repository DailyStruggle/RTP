# --- 言語マッピング: worlds/*.yml ---
# 内部ワールドキー名（左側）を `worlds/` 配下のすべてのファイルで操作者に見える
# キー名（右側）に対応付けます。
# 名前を改名・翻訳する場合は右側のみを編集してください。
# 左側はコード定数なので変更しないでください。
# このファイルが存在しない場合、プラグインは左右が同一の英語版を再生成します。
region: "リージョン"
requirePermission: "権限要求"
override: "上書き"
version: "version"
