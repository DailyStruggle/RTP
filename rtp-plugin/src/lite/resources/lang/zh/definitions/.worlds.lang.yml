# --- 语言映射: worlds/*.yml ---
# 将内部世界键名（左侧）映射为 `worlds/` 下所有文件中用户可见的键名（右侧）。
# 仅可修改右侧以重命名或翻译选项；左侧为代码常量，请勿更改。
# 若此文件缺失，插件会自动重新生成左右一致的英文默认映射。
region: "区域"
requirePermission: "需要权限"
override: "覆盖"
version: "version"
